# EneTrend 実装計画

## 目的と進め方

Health Connect に蓄積された健康データを読み取り、カロリー収支と体重変化の関係を端末内で分析・可視化する。食事・運動・体重の入力や Health Connect への書き込みは行わない。

カロリー収支はアプリ全体で **摂取カロリー − 総消費カロリー** に統一する。負は消費超過、正は摂取超過。欠損値と実測値の 0 を区別する。

Git 運用と作業範囲は [AGENTS.md](AGENTS.md) に従う。原則として上から進め、ユーザーが指定した項目を優先する。各項目を1回の作業・feature ブランチで扱う目安とし、フェーズ全体を無断で一括実装しない。完了条件と検証結果を確認してチェックを更新する。未決事項は該当項目の実装前にユーザーと決定する。

## 現在の状態と参照先

MVP（Phase 0〜9）は完了。GitHub生成APKのダウンロード・インストールもユーザー確認済み。Phase 10の10.1〜10.6は完了。10.7は低優先度としてMVP対象外へ移し、既存の将来拡張4件と併せて管理する。

- 利用方法・現行仕様の概要・開発と検証手順: [README.md](README.md)
- 完了済みの計画、詳細な確定仕様（欠測・補間・移動平均・グラフ）、検証履歴、MVP完了条件: [MVP開発履歴](docs/mvp-history.md)

実装時は関連する確定仕様と既存コードを確認する。履歴内の未実装・未確認という記述は当時の状態であり、新規タスクとして扱わない。

## Phase 10: MVP後の配布・画面・再取得の改善

各番号を個別に依頼・実装・検証できる単位とする。10.1は独立、画面整理は10.2→10.3→10.4の順、累積仕様は10.5→合意後に10.6の順。10.7は優先度を下げ、MVP対象外へ移動した。10.1はGitHub上でのAPK単体取得・インストールのユーザー確認を含めて完了。10.2・10.3も完了。10.5の仕様検討は完了。10.6も実装・検証完了。10.4も実装・検証完了。10.7はMVP対象外で未着手。

- [x] 10.1 GitHub artifactをZIPなしのAPK配布へ変更する。
  `.github/workflows/debug-apk.yml`のupload-artifact v4をv7へ更新し、`archive: false`でAPK単体を保存する。既存のSHA固定方針に合わせる場合は公式v7の対応SHAを確認する。公式仕様では単一ファイルのみ対応し、`name`は無視されてファイル名がartifact名になるため、アップロード用APK名にcommit SHA・run attemptと`.apk`を含める。`compression-level: 0`だけではZIPをなくせないため不要な設定を整理する。master push起動・14日保存・ファイルなしで失敗・最小権限を維持する。
  完了条件: workflow構文・runner互換性・ビルドを検証し、ユーザーのmaster更新後にGitHubからZIP展開なしでAPKを取得・インストールできることを確認する。READMEの入手方法も更新する。参照: [upload-artifact公式仕様](https://github.com/actions/upload-artifact#inputs)。
  実装・ローカル検証済み: upload-artifactを公式v7のcommit 043fb46d1a93c77aae656e7c1c64a875d1fc6a0a（Node 24）へ固定。既存Ubuntu 24.04のGitHub-hosted runnerを継続使用する。ビルドAPKをcommit SHA・run attempt付きの.apk名へコピーし、その単一パスをarchive: falseで保存する。無効になるnameと不要なcompression-levelを削除し、14日保存・master push・ファイルなしで失敗を維持した。READMEの入手手順も更新。actionlint、:app:assembleDebug --no-daemon、pre-commit（gitleaks）が成功。アプリコードは変更しておらず、JVM・Android回帰とlintは再実行していない。完了確認: ユーザーからGitHubでのAPK直接ダウンロード・インストールが正常に動作したとの報告を受領し、10.1を完了とする。この完了状態の更新では、ユーザー指示に従いテストを再実行していない。

- [x] 10.2 メイン画面の重複した見出し・計算式を整理する。
  `DashboardScreen`の上部アプリ名と「収支 = 摂取 − 消費」の常設行を削除してグラフに使う領域を確保する。`HealthConnectionScreen`にも同じアプリ名見出しがあるため、接続画面を含めた削除範囲を実装前に確認する。ランチャーのアプリ名・プライバシー説明は変更対象にしない。収支の計算・符号は変更せず、正負の意味は凡例とREADMEで確認できるようにする。
  完了条件: 通常・loading・空データ・権限不足で余白や状態説明が不自然にならず、API 34の縦横・文字拡大で確認する。関連する画面テストを更新する。

- [x] 10.3 グラフの凡例を初期状態で折りたたむ。
  `DashboardChart`の凡例をタップで開閉できる表示にし、通常は折りたたむ。4系列の識別、正負の色、左右軸・期間累積の尺度、実測と補間の説明は展開時に参照できるよう保持する。軸ラベルや選択日の数値まで隠さない。開閉状態の期間切替・画面復元時の扱いは実装時に明確にする。
  完了条件: 初期折りたたみ→タップで展開→再タップで閉じる動作をUIテストで確認。開閉状態を読み上げ可能とし、グラフの日付選択と競合せず、ライト／ダーク・文字拡大でも操作できる。

10.2・10.3完了: メイン・接続画面のアプリ名見出しとメインの計算式行を削除。凡例は初期折りたたみとし、タップで開閉、読み上げ用の状態を追加した。凡例に正負の意味を残し、文字拡大時は左右の列幅を確保して折り返す。軸・累積尺度の説明・日付詳細は常時表示する。日付選択中は開閉を維持し、グラフ単体の保存復元に対応。実画面の期間変更・再取得・復帰・回転でグラフがいったん非表示になれば折りたたみに戻る。READMEに明記した。両変更後にまとめてJVM105件・API 34 Android39件の回帰を実施し成功。追加テストの座標指定のコンパイルエラーは修正済み。文字拡大の列幅調整後、関連Android10件とbuild / lintを再実行して成功。lintはエラー0・既存警告16件。通常・空データ・loading・権限不足・回転は既存UIテストでも確認し、凡例開閉・状態復元・両テーマ・日付選択を追加検証。API 33手動確認は方針により省略。

- [x] 10.4 選択日の詳細を状態アイコンで見分けやすくする。
  `DashboardSelection`の文字列による詳細表示と`DashboardScreen`を整理し、摂取・消費・日別収支・期間累積・体重・移動平均の値あり／欠測・未算出をアイコンと短いラベルで示す。実測0を欠測にせず、補間・推定、不完全な参考累積（10.6の仕様）、平均日数不足も区別できるようにする。アイコンだけ・色だけに依存せず数値と単位、読み上げ説明を維持する。現行データモデルを再利用し、この項目では累積計算を変更しない。
  完了条件: 実測・0・補間・欠測・未算出・日数不足の日付詳細を単体／UIテストで確認。タップ・スライダー・アクセシビリティ操作で同じ日付の全系列が切り替わることを回帰確認する。

10.4完了: DashboardSelectionに6項目の詳細行と状態モデルを追加し、DashboardDetailsで数値・単位、状態アイコンと短いラベルを表示する。通常値はチェック付き丸、欠測／未算出は横線付き丸、補間・推定・参考累積・日数不足は三角で区別し、参考累積と推定、値ありと平均日数不足を併記する。0を欠測扱いせず、補間の前後日付・欠測除外日数・平均実測日数・長期欠測の説明を保持。色やアイコンだけに依存せず、画面と読み上げが同じ状態モデルを使用する。計算ロジック・依存ライブラリは変更なし。JVM3件追加で全111件、文字2倍・両テーマのUIテスト1件追加と日付選択テスト更新でAPI 34全41件が成功。test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaks（新規3ファイルの直接走査を含む）・差分確認が成功。lintはエラー0・既存警告16件。API 34の実データ画面でも状態アイコンと配置を確認し、READMEへ使い方を反映した。API 33手動確認は方針により省略。

10.4表示調整: ユーザー指示により、欠測／未算出は文字の右側に本文内アイコンを表示し、同じ状態を次行へ重複表示しない。数値の隣にはライト／ダーク別の緑のチェック付き丸を表示し、「値あり」の補足行は削除。追加指示により値・欠測・未算出のアイコンをすべて右側に統一し、参考情報のアイコン位置は維持する。補間・推定・参考累積・日数不足の補足は維持する。本文の読み上げと状態情報も維持。変更後のbuild / lint、API 34の詳細表示・日付選択6件、pre-commit / gitleaks・差分確認が成功。計算変更はなく、全体回帰は直前の111件／41件の結果を再利用した。

- [x] 10.5 欠測日以降の期間累積収支の仕様を再確認し、方針を決定する（仕様検討のみ）。
  ユーザー合意済み。検討時点では欠測日以降を未算出としていたが、以下の方針へ変更する。実装・検証結果は10.6を参照。
  * 摂取カロリーが欠測、または既存ルールで補間できない総消費カロリーが欠測の日は、日別収支を欠測・未算出のまま保つ。日別の棒を0として描かない。累積への寄与だけを0とし、その日の累積は前日値を維持する。摂取・消費の記録自体を0へ置換したり、摂取を推定補完したりしない。
  * 期間開始を0 kcalとし、算出できる日の日別収支を日付順に加算する。例: 日別が−300、欠測、＋200、−400なら期間累積は−300、−300、−100、−500。2日目以降は参考値として扱う。
  * 最初の欠測日以降は、線種（破線など）と詳細で不完全な参考値であることを明示する。系列名は「期間累積収支」を維持し、「欠測日を除いた参考累積」「欠測N日を除外」等の説明を添える。Nは期間開始から選択日までの除外日数。欠測日の実際の収支や誤差の方向・大きさを推定できたとは扱わない。消費補間由来の推定と、欠測日の除外による不完全性は区別して保持する。
  * 表示期間の全日で日別収支が算出できない場合は、累積線・開始0の点を表示せず「算出できる日がありません」と説明する。実測収支が0の日は算出可能日として扱う。期間内に算出可能日がある場合、開始直後の欠測は開始0を維持する参考値となる。
  * 後日記録が追加されたら、再取得時に期間全体を再計算する。欠測が解消した区間は不完全表示を解除するが、残る欠測や補間由来の推定表示は維持する。表示期間の変更時も新しい開始日を基準に、累積・除外日数・表示状態を再計算する。
  * 体重・消費の既存の最大6日補間、移動平均、累積のkg軸への位置合わせは変更しない。日別の欠測を跨いで棒や記録を生成しない一方、参考累積の線は欠測日にも前日値を保持して継続する。

- [x] 10.6 合意した欠測後の累積仕様を計算・グラフ・詳細へ反映する（10.5の確定仕様に基づく）。
  10.5の確定方針に従い、既存の計算層・グラフ用モデル・線描画・小計／状態表示を必要最小限で更新する。UIに累積計算を埋め込まない。既存の算出可能日の小計との重複表示を整理し、不完全な参考累積を完全な累積と誤認させない。10.4のアイコン表示が実装済みなら整合させ、未実装ならその実装を先取りせず状態情報と文字説明を維持する。
  完了条件: 摂取欠測・補間不能の消費欠測・両方欠測、連続／先頭／末尾欠測、全日未算出、算出可能な0、正負混在、消費補間と欠測除外の併存を単体テストで確認する。−300、欠測、＋200、−400の例、表示前データの除外、期間変更による累積と除外日数のリセット、後日追記による値と不完全表示の復旧も確認する。UI／API 34で日別の欠測表示、参考累積の継続線・凡例・詳細、全日未算出で開始点を含め累積を描かないことを検証する。READMEを新仕様へ更新し、旧仕様は履歴として区別する。

10.6実装: CalorieBalanceCalculatorで期間内に算出可能日があれば欠測を加算せず累積を継続するよう変更。日別未算出とmissingDates・estimatedDatesを保持し、参考累積の破線・白抜きと除外日数の詳細を追加した。重複する小計表示を削除。全日未算出なら累積線・開始点・尺度説明を非表示にし、算出可能日なしを案内する。体重補間・移動平均・kg軸変換は維持。READMEを新仕様に更新し、旧仕様はMVP履歴として参照可能。JVMテスト3件追加、既存計算・座標テストを更新し、全108件成功。Androidでは参考累積の継続・日付選択を更新し、全日未算出の線／開始点非描画と記録追加後の復旧テストを追加。test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit（gitleaks）・差分確認が成功。lintはエラー0・既存警告16件。API 34でAndroid全40件が成功。初回は既存の接続状態テストでActivity終了待ちがタイムアウトしたため、エミュレータをデータ保持のまま再起動して全件を再実行し成功した。欠測・記録追加のUI検証はテスト用データソースで行い、実Health Connectの読み取り・再確認・回転も既存回帰で確認した。健康データの書き込みは行っていない。API 33手動確認はプロジェクト方針により省略。

実装時は各項目に該当するtest / lint / build、pre-commit / gitleaksを実施する。各項目の実行結果・未確認事項は上記の完了記録に残す。

## Phase 11: グラフの軸と補足表示の整理

今回追加した仕様を11.1〜11.4の個別の実装・検証単位で管理する。計画のみで未着手。完了済み10.4等の記録は当時の実装履歴として保持し、今後の表示変更は本フェーズに従う。長期間表示L1〜L8の実装を前提としない。

- [ ] 11.1 左の日別収支軸を0中心・上下対称の切りのよい目盛りにする。
  `DashboardChartProjector`の尺度生成と`DashboardChart`のラベル生成を対応させ、目盛り間隔は最小5 kcal、5 kcalの整数倍の切りのよい値とする。小数や端数のラベルを出さず、データの幅に応じて間隔を広げる。軸の上下限は同じ絶対値とし、0をプロットの中央へ配置する。ラベルだけを丸めて実際の座標とずれる実装にしない。収支の実値や詳細表示の精度は変更しない。
  完了条件: 正のみ・負のみ・正負混在・微小値・0のみ・空データ・大きな値で、5 kcal未満の目盛り間隔がなく、0が中央、上下対称、棒が範囲内となることを`DashboardChartTest`等で確認する。API 34でラベルとグリッドの一致、両テーマ・文字拡大を確認する。

- [ ] 11.2 右の体重軸を最も左に描画される体重値を中心とする尺度へ変更する。
  `DashboardChartProjector`で期間内の最初にプロットされる体重の日付・値を求め、その値を右軸の中央とする。表示期間変更時は基準を再選定し、日付選択だけでは軸を動かさない。体重・移動平均・kgへ位置合わせした期間累積が収まるよう、中心から上下同じ幅を確保する。単一プロットと既存の7,000 kcal/kgの尺度連動を維持する。
  決定事項: 左端の体重が補間値でもその値を中心にする。実測・補間の体重がなく移動平均だけがある場合は、最も左にプロットされる日の移動平均値を中心にする。体重・移動平均ともにない場合は、プロットする期間累積収支から表示範囲を決め、右のkg目盛りの中心を0とする。これは描画のための尺度であり、0 kgの体重記録や推定体重を生成しない。既存の独立累積尺度・「体重データなし」表示との整合も実装対象とする。
  追加決定事項（ユーザー合意済み）: 累積0の縦位置も右軸中心へ統一する。先頭の体重が補間ならその補間値、移動平均だけなら最初の移動平均値を基準に7,000 kcal/kgで連動させる。後続の実測を優先する現行の累積基準と、移動平均のみの場合の独立累積尺度はこの仕様へ変更する。体重・移動平均がない場合も累積0は右軸中心の0に合わせ、同じ換算係数で尺度を連動させる。体重・移動平均・算出可能な累積がすべてない場合は右軸の数値を「—」とし、架空の値や累積開始点は描画しない。11.2の仕様上の未決事項は解消済みで、実装は別途依頼後に行う。
  完了条件: 先頭欠測・先頭補間と後続実測・1点のみ・体重なし・移動平均のみ・全系列なし・大きい正負の累積・期間変更をテストする。中心値がラベルと座標で一致し、累積0も中央となり、7,000 kcal/kgの尺度連動を保つことを確認する。実値を丸めて中心をずらさず、全系列を欠けずに表示でき、全系列なしでは右目盛りが「—」となる。

- [ ] 11.3 参考情報を値・欠測と同じ行のアイコンから開けるようにする。
  `DashboardDetails`の別行の参考状態ラベル・補足文を、値／欠測表示と同じ行にあるタップ可能な参考情報アイコンから表示する方式へ変更する。`DashboardSelection`の状態・補足モデルを再利用し、補間元の日付、推定、欠測除外日数、平均の実測日数・日数不足・長期欠測の理由を詳細で読めるようにする。値ありの緑チェックと欠測／未算出の右側アイコンは維持し、参考情報の操作と区別する。参考情報が複数ある場合も全内容へアクセスできるようにする。
  ダイアログ等の表示方式は実装時に既存UIに合わせて決める。通常表示でも補間・推定・不完全な参考値であることがアイコンと読み上げで判別でき、実測と誤認させない。日付変更時は古い日の詳細を表示し続けない。
  完了条件: 値あり／欠測／未算出と参考情報の併存、複数の参考状態、開閉と戻る操作、日付変更を`DashboardDetailsTest`・選択関連UIテストで確認する。タップ領域、読み上げ、両テーマ・文字拡大で操作でき、補足が別行に常設・重複表示されない。

- [ ] 11.4 グラフ下の期間開始・累積0基準・換算係数の常設行を削除する。
  `DashboardChart`の「期間開始…0.0 kcal」「累積0の基準…」「期間累積は右軸の1 kg幅＝…kcal幅」の行を削除する。累積の開始0の計算・プロット、基準体重への位置合わせ、換算係数の適用は維持する。尺度や体重予測ではないことの説明は、既存の折りたたみ凡例で必要時に確認できるよう整理し、グラフ下へ別の常設説明を増やさない。体重なし時の独立尺度の案内と全日未算出の案内は今回の3行削除と区別する。
  完了条件: 指定の3行が通常画面に出ず、凡例の開閉、累積起点の描画、体重なし・全日未算出、日付詳細が維持されることをUIテストで確認する。READMEの表示説明を更新する。

実施順序の目安: 11.1 → 11.2（仕様合意済み）→ 11.3 → 11.4。11.3は軸変更から独立して実施できる。11.4の説明は11.2で確定した基準と整合させる。各項目で関連テストと該当するlint / build / pre-commit / gitleaksを実施し、まとめて実装を依頼された場合は変更後に回帰確認をまとめて行う。長期間表示L6でも、このフェーズで確定・実装した軸と説明の方針を引き継ぐ。

## MVP 対象外

以下は未着手の将来拡張。ユーザーが作業対象として指定した段階で仕様を確認して実装する。記載順は既存計画を維持しており、新たな優先順位の決定ではない。欠測・推定の基本表示と実測体重・移動平均の同一時間軸表示は実装済み。

- [ ] 10.7 Health Connectのデータをその場で再読み込みできる操作を明確にする。
  現在も下部の「再確認」→`MainActivity.refresh`→`readVersion`更新→`DashboardRoute`再取得が実装済み。ただし通知は権限確認の結果であり、データ取得完了とは異なる。既存処理を再利用し、即時の手動再取得を分かりやすく提供する。ボタンの改名・専用操作・スワイプ更新のどれにするかは実装前に確認し、既存の下部配置方針との関係も明確にする。
  操作後に権限確認と取得を開始し、loading・取得成功・空データ・失敗を区別する。表示期間・平均期間・範囲内の選択日を維持し、連打時の重複取得や古い結果の上書きを防ぐ。「即時」はHealth Connect内の最新データを読み直す意味とし、記録元アプリからの同期を強制する機能にはしない。
  完了条件: `DashboardPeriodTest`・`HealthReconnectTest`等を拡張して、同じ期間での再取得、データ更新の反映、失敗からの復帰、権限取消、連打を検証する。API 34で操作と通知を確認し、READMEを更新する。健康データの書き込みは追加しない。
  優先度: ユーザー指示により低優先度へ変更。MVP対象外とし、別途指定されるまで実装しない。

### 長期間表示: 3か月・半年・1年・3年・5年

以下のL1〜L8を個別の依頼単位とする。MVP対象外のまま管理し、この計画の追加では実装を開始しない。既存の7日・14日・30日、初期表示30日、移動平均の7日・14日・30日は維持する。表示期間と移動平均の計算期間を混同しない。10.7の手動再取得改善は前提としない。

調査結果: `MainActivity`と`DashboardScreen`は表示日数を保持・選択し、`DashboardLoader`は7／14／30日に限定して取得範囲と計算用の先行範囲を作る。`HealthDataRepository.readDailyCalories`は日ごとに集計を要求し、体重はページ単位で取得する。`CalorieBalanceCalculator`・`WeightTrendCalculator`が日単位の計算、`DashboardChartProjector`が座標生成を担当する。外部チャートライブラリはなく、Compose Canvasで全日分を描画し、日付をタップ／スライダーで選択する構成。年単位では取得回数、計算時の集合コピー、棒の幅と日付選択精度を確認する必要がある。

Health Connectは通常、最初の権限付与より30日前までの履歴を読み取れる（常に「直近30日だけ」という制限ではない）。それ以前には履歴読み取り権限が必要で、現行アプリは要求していない。権限があっても記録元に存在しないデータは取得できない。参照: [Health Connectの履歴読み取り・ページング仕様](https://developer.android.com/health-and-fitness/health-connect/read-data)。

- [ ] L1 長期間表示の期間境界・履歴不足・表示密度の仕様を決定する（仕様検討のみ）。
  3か月／半年／1年／3年／5年を暦月・暦年で遡る期間として扱うか、開始日の算出方法と今日の包含をユーザーと決める。月末・うるう日の具体例を残し、固定90日・180日等へ無断で置き換えない。履歴権限の未対応・拒否・一部期間だけ読み取り可能な場合に、選択を制限するか、制約を示して部分表示するかも決定する。取得不能部分を通常の欠測として参考累積へ混入させたり、表示期間の開始を黙って変更したりしない。
  長期間で日別の棒が潰れる場合の表示方針（全期間表示、拡大・スクロール、描画用の間引き等）と日付選択方法を比較し、必要最小限の方式を決定する。拡大した可視範囲と計算対象の選択期間を区別し、拡大操作だけでは期間累積の起点を変更しない。完了条件: 期間境界・履歴不足・表示方式の合意と受入例をTODOへ記録し、未決のまま依存する実装へ進まない。

- [ ] L2 暦月・暦年を扱える表示期間モデルと日付範囲計算を追加する。
  前提: L1。整数の日数だけに依存する期間指定を最小限で拡張し、既存の`HealthDataRange`へ変換する処理をUIから分離する。終了日を含まない範囲・ユーザーのタイムゾーンを維持し、補間／移動平均用の取得範囲と表示範囲を分ける。長期間の選択肢の公開はL7で行う。
  完了条件: 既存7／14／30日と新しい各期間について、月末・うるう年・年越し・夏時間を含む境界をJVMテストで確認する。計算用の先行日が表示期間へ混入せず、移動平均期間の変更で表示範囲が変わらない。

- [ ] L3 Health Connectの履歴読み取り可否と追加権限を扱う。
  前提: L1。使用中のHealth Connect APIで履歴読み取り機能の利用可否を確認し、必要な場合に追加の読み取り権限を要求・再確認する。現在の3種類の読み取り権限と、履歴へのアクセス可否を区別して状態管理する。拒否・取消・未対応・再インストール後を扱い、既存の短期間表示を不必要に利用不能にしない。書き込み・バックグラウンド権限は追加しない。
  完了条件: 判定・権限不足状態をテストし、対応するAPI 34環境で許可／拒否／取消を確認する。履歴権限が使えることをOSバージョンだけで決めつけず、履歴不足時はL1で決めた案内を表示する。

- [ ] L4 長期間のデータ取得と取得範囲の状態を整備する。
  前提: L2・L3。現在の日数に比例するカロリー集計要求を見直し、日単位バケットの集計APIや範囲分割の対応・制約を確認して方式を選ぶ。ローカル日付境界・単位・データソース間の二重集計防止を維持する。体重のページングを維持し、`DashboardLoader`が体重だけを必要とする取得で不要な全期間カロリー集計を繰り返す点も整理する。
  現在の`historyLimited`と先行範囲の権限エラー時フォールバックだけでは、長い表示範囲自体のアクセス不足を表現できないため、取得不能と記録なしを区別してL1の方針を反映する。キャンセル・権限取消・途中失敗・レート制限を扱い、失敗した日やページを正常な空データにしない。
  完了条件: 日別集計の同値性、月／年境界、複数ページ・重複・途中失敗、切替時のキャンセルをテストする。5年分の要求回数と取得時間を確認し、必要性のない並列大量取得・永続キャッシュ・新規基盤は導入しない。

- [ ] L5 長期間の計算と状態保持を検証し、必要な性能改善を行う。
  前提: L2（取得状態との結合確認はL4後）。期間累積は新しい選択期間の開始0から再計算し、表示前の収支を加算しない。10.5／10.6の参考累積、最大6日補間、実測値だけの移動平均を維持する。各日での欠測日・推定日の集合コピーや平均計算の走査を測定し、長期間で問題になる処理だけを改善する。履歴へのアクセス制限は記録の欠測と区別する。
  完了条件: 5年分の合成データで時間・メモリを測定し、完全／全欠測／長期欠測／補間混在を確認する。期間変更時の起点・除外日数リセット、正負・0・日付順・期間外データ除外、後日記録追加をJVMテストで確認する。移動平均の窓や補間上限を表示期間に合わせて勝手に拡大しない。

- [ ] L6 単一プロットの長期間描画と日付選択を対応させる。
  前提: L1・L2・L5。既存Canvasと`DashboardChartProjector`を活用し、L1で合意した密度・操作方針を実装する。日別の棒、期間累積、体重、既存移動平均を同じプロット領域・共通日付X軸に維持し、年を跨ぐラベルを識別可能にする。左日別kcal軸・右kg軸と7,000 kcal/kgの累積尺度を維持し、長期累積による体重線の見づらさも確認する。尺度変更が必要なら別途仕様判断を受ける。
  描画を間引く場合も計算・選択日の詳細は元の日別値を使い、欠測の線切れや参考／補間表示を消さない。選択操作で任意の日を正確に参照でき、スライダーだけに数年分の選択を依存させない。
  完了条件: 同日の全系列の対応、端点・年越しの日付選択、欠測境界・開始0・線種を単体／UIテストで確認する。5年表示の操作応答と描画を測定し、既存短期間の見た目・選択・読み上げを維持する。

- [ ] L7 長期間の選択肢を画面・保存状態・再取得へ接続する。
  前提: L2〜L6。3か月・半年・1年・3年・5年を既存の下部期間選択へ追加し、画面幅・文字拡大でも操作できる配置にする。`MainActivity`・`DashboardRoute`・`DashboardLoader`を期間モデルで連動させ、X軸と全系列を同じ範囲へ更新する。移動平均期間は独立して選択する。
  完了条件: 期間切替、連続切替中のキャンセルと古い結果の排除、回転・状態復元・アプリ復帰・既存の再確認操作をUIテストで確認する。範囲変更後の選択日を一貫して扱い、履歴権限不足・loading・失敗・空データでも選択範囲と状態説明が一致する。

- [ ] L8 長期間表示の結合検証と利用説明を仕上げる。
  前提: L3〜L7。既存短期間と各長期間を含む回帰、最大5年の取得・計算・描画・日付選択をまとめて検証し、計測条件と結果を記録する。API 34でライト／ダーク、縦横、文字拡大、履歴権限の各状態を確認する。API 33手動確認は既存方針に従い省略する。
  完了条件: 該当するtest / lint / assembleDebugとAndroidテスト、pre-commit / gitleaksを通し、READMEへ期間境界・履歴制限・長期の参考累積の注意と操作を反映する。大量データは原則テスト用データソースで検証し、保存済みの投入ツールを使う場合もエミュレータ限定を守る。健康データをログや端末外へ送信しない。

実施順序: L1 → L2・L3 → L4・L5 → L6 → L7 → L8。各項目の前提を満たしたうえで個別に着手する。ローリング収支、参考体重変化の数値表示、換算係数の設定UI、新たな補完方式は本計画に含めない。

### その他の将来拡張

- [ ] 将来拡張: 直近30日などのローリングカロリー収支を追加する。
  表示期間の先頭を基準とする期間累積収支と区別し、各日の移動窓内の収支を計算する。窓の日数、表示方法、計算に必要な先行データ、欠測時の扱いを実装前に決定し、窓境界・欠測をテストする。
- [ ] 将来拡張: 期間累積収支を体重変化の参考値へ換算する。
  既存の7,000 kcal/kgによる描画上の位置合わせとは別に、参考体重変化を値として提示する機能。表示する値・換算方法と係数の適用範囲を実装前に決定する。実測体重や確定的な予測と混同させず、推定であることと欠測・補完由来の不確かさを示し、換算と欠測時の扱いをテストする。
- [ ] 将来拡張: グラフの kcal/kg 換算係数をユーザーが変更できる設定UIを追加する。
  初期値7,000のコード上の係数を再利用し、入力範囲・保存・初期値への復帰を実装前に決定する。変更が描画に反映され、収支の実値や健康データを変更しないことを検証する。

ユーザーアカウント、クラウド同期、独自バックエンド、食事・運動・体重入力、Health Connect への書き込み、広告、analytics、telemetry、SNS 共有、CSV export、複雑な設定画面は必須計画に含めない。追加する場合は別途ユーザーの明確な要件に基づいて計画する。
