# EneTrend 実装計画

## 目的と進め方

Health Connect に蓄積された健康データを読み取り、カロリー収支と体重変化の関係を端末内で分析・可視化する。食事・運動・体重の入力や Health Connect への書き込みは行わない。

カロリー収支はアプリ全体で **摂取カロリー − 総消費カロリー** に統一する。負は消費超過、正は摂取超過。欠損値と実測値の 0 を区別する。

Git 運用と作業範囲は [AGENTS.md](AGENTS.md) に従う。原則として上から進め、ユーザーが指定した項目を優先する。各番号付き項目を1回の作業・feature ブランチで扱う目安とし、フェーズ全体を無断で一括実装しない。完了条件と検証結果を確認してチェックを更新する。未決事項は該当項目の実装前にユーザーと決定する。

## 現状（2026-09-13 確認）

| 対象 | 確認した状態 |
| --- | --- |
| 文書 | `AGENTS.md` あり。`README.md` と既存 `TODO.md` はなし |
| モジュール | `settings.gradle.kts` のプロジェクト名は `enetrend`、モジュールは `:app` のみ |
| package / namespace / applicationId | `io.github.puvon.enetrend`。テーマは `.ui.theme` |
| SDK | minSdk 28、targetSdk 37、compileSdk 37 |
| ビルド | Gradle Wrapper 9.5.0、AGP 9.3.2。Java ソース・ターゲット互換性 11（Gradle 実行 JDK の指定とは別） |
| Compose | 有効。Compose plugin の Kotlin バージョン定義 2.2.10、BOM 2026.02.01。Material 3、Activity Compose、UI・preview・tooling を使用 |
| 画面 | `MainActivity` 内の `Greeting` が `Hello Android!` を表示。`GreetingPreview` と `EnetrendTheme`、Color / Type のテーマ定義あり |
| データ・計算 | Health Connect 依存・権限・取得処理なし。集計、収支、体重、移動平均、グラフ、専用の画面状態管理は未実装 |
| テスト | `ExampleUnitTest` は加算確認、`ExampleInstrumentedTest` は package 確認のみ。JUnit と Android / Compose テスト依存は設定済み |
| Manifest | 起動 Activity は `MainActivity`。`allowBackup=true`、バックアップ XML はサンプル状態 |
| セキュリティ検証 | `.pre-commit-config.yaml` に gitleaks v8.30.0 の `gitleaks-system`、`pass_filenames: false` を設定済み。独自 gitleaks 設定は確認されず |
| 除外設定 | ルートと `app` に `.gitignore` あり。`local.properties`、ビルド生成物等を除外。秘密情報全般が除外済みとはみなさない |

設定・ソースの存在確認と、ビルドや実機での動作確認は区別する。サンプルテストの存在を製品ロジックのテスト完了とは扱わない。

## Phase 0: 現状確認・基盤確認

- [x] 0.1 既存ドキュメント、全 Gradle 設定、Manifest、ソース・テストを確認し、上記の現状を整理する。
- [x] 0.2 単一 app モジュール、Compose / Material 3 の画面基盤、JVM / Android テスト基盤、pre-commit の設定済み部分を確認する。
  新規プロジェクト作成や Compose 導入を重複して行わない。
- [x] Wrapper の `tasks --all --console=plain` を実行し、検証タスクの存在を確認する。
  一覧取得は成功。製品コードの test / lint / build 成功を意味するものではない。
- [x] 0.3 開発環境と基準となる検証結果を確認する。
  Wrapper のタスク一覧で検証タスクを確認し、test / lint / debug build を実行する。失敗時は既存問題と環境問題を区別して報告し、SDK や依存バージョンを根拠なく変更しない。pre-commit とシステムの gitleaks の利用可否・hook 動作も確認する。

### 0.3 の検証結果（2026-09-13）

`feathre/base2` 上で `.\gradlew.bat test lint assembleDebug --continue --console=plain` が成功。既存 JVM テスト1件は失敗・エラー・スキップなし。debug APK の組み立ては UP-TO-DATE を含め成功した。Android test / instrumentation test は今回未実施。

lint はエラー0件・警告16件。内訳は Activity の重複 label 1件、依存バージョンの更新通知8件、未使用の色リソース7件。既存コード・設定に対する警告として記録し、この基盤確認では変更・抑制していない。詳細は生成レポート `app/build/reports/lint-results-debug.html` を参照する。

既存 `.git/hooks/pre-commit` は pipx の pre-commit 専用 Python を参照している。pre-commit と gitleaks はこの実行環境の PATH では見つからなかったが、インストール済みだった。hook の Python から `-m pre_commit run --all-files` を実行した初回は `Executable gitleaks not found` で失敗。WinGet 配下の既存 gitleaks 8.30.1 のディレクトリを実行プロセスの PATH に追加して再実行し、設定済み `gitleaks-system` が Passed となった。設定の参照 rev は v8.30.0、実際のシステム実行版は 8.30.1。hook や検出の迂回、永続 PATH の変更、新規インストールは行っていない。

同じ実行環境で再検証する場合は、既存 hook の Python とインストール済み gitleaks を解決できる環境を使用する。commit 自体は実行していないため、commit 時の自動起動は未検証。設定済み hook を pre-commit から実行した結果を基準とする。

## Phase 1: Health Connect 接続基盤

完了（2026-09-14）: 1.1〜1.3 の実装・自動検証、API 34 のユーザーによる手動確認と再確認ボタン修正後の OK 報告に基づき、ユーザーの指示により Phase 1 を完了とする。API 33 の手動テストはプロジェクト共通方針により省略する。

* `connect-client:1.1.0` を追加し、`AndroidHealthConnection` で利用可否・クライアント生成を UI から分離した。
* 栄養・総消費カロリー・体重の読み取り権限のみを宣言・要求する。Android 13 以下の rationale Activity と Android 14 以降の permission usage alias を定義し、用途説明・プライバシー画面を追加した。
* 起動・画面復帰・権限要求前・結果受信後に権限を再確認し、未許可・一部許可・取り消し・確認失敗を接続済みと区別する。インストール／更新、再要求、設定、再確認の導線を追加した。
* 2026-09-14: API 34 の手動確認で「再確認」の反応が分かりにくいとの報告に対応し、手動再確認の完了・失敗を Snackbar で通知するよう変更した。同じ権限状態でも結果を表示する。API 34 で実 Activity の再確認ボタンから通知までを確認するテストを追加し、Android テスト5件と test / lint / assembleDebug が成功。変更後の API 33 は未再検証。
* 健康データの取得は Phase 2 で実装する。取得直前の権限再確認と取得中の権限喪失への対応も、その取得処理に組み込むこと。
* JVM テストで利用不可・更新必要時に権限照会しないこと、拒否・部分許可、取り消し・再付与、例外からの復帰、キャンセル伝播を検証する。Android テストには権限定義の一致、権限要求・設定・更新ボタンの導線確認を追加した。

手動確認の対象: API 34 の利用可否、権限許可・拒否・一部許可・取り消し後の復帰、プライバシー導線、画面回転・バックグラウンド復帰。結果は以下のユーザー報告に基づく。API 33 の手動テスト（未導入・更新案内や Android 13 向けプライバシー導線を含む）は、AGENTS.md のプロジェクト共通方針により省略し、完了の阻害条件にしない。

2026-09-14: ユーザーから API 34 の手動確認は概ね OK、再確認ボタン修正後も OK との報告を受領。個別手順すべての実施を確認した記録とは区別する。API 33 の手動テスト省略は、今回限りではなく今後のプロジェクト共通方針として決定した。

2026-09-14 に `enetrend_api34`（emulator-5554、API 34 / Android 14）で `.\gradlew.bat :app:connectedDebugAndroidTest --console=plain` を実行し、4件成功・失敗0件・スキップ0件。package 確認、部分許可状態の要求・設定ボタン、更新必要状態の案内・ボタン、Manifest と要求権限が3種類の読み取りのみで一致することを確認した。画面テストは状態とコールバックを渡して検証するもので、Health Connect のシステム権限画面での許可・拒否・取り消し操作を検証したものではない。システム画面の手動確認結果は、上記のユーザー報告として別に記録する。

2026-09-14 に `enetrend_api33`（emulator-5554、API 33 / Android 13）でも同じ Android テストコマンドを実行し、4件成功・失敗0件・スキップ0件。検証対象は API 34 と同じ。Health Connect のシステム画面での許可・拒否・取り消しは今回も検証対象外。

自動検証: `test lint assembleDebug :app:assembleDebugAndroidTest --continue --console=plain` が成功。JVM テストは既存1件＋追加5件が成功した。pre-commit の `gitleaks-system` と、新規ファイルを含む `app/src`・`TODO.md` の gitleaks 走査も通過した。Phase 0.3 と同じく実行プロセス内の PATH で既存 gitleaks を解決した。

参照した公式仕様: [導入・権限・プライバシー導線](https://developer.android.com/health-and-fitness/health-connect/get-started)、[リリース情報](https://developer.android.com/jetpack/androidx/releases/health-connect)。公開時には Play Console の権限申告・プライバシーポリシーと一致させる。

- [x] 1.1 公式 Health Connect 依存と最小限の接続構成を導入する。
  既存の version catalog と app 構成を使用する。採用時の公式 API と SDK 互換性を確認し、利用可否判定・クライアント生成を UI から分離する。対応環境、未導入・更新が必要な環境、利用不可を扱い、端末で確認する。
- [x] 1.2 3種類のデータの読み取り権限と権限要求導線を実装する。
  `NutritionRecord`、`TotalCaloriesBurnedRecord`、`WeightRecord` に必要な権限だけを定義する。公式仕様に沿う Manifest 設定と権限の用途説明・プライバシー導線を整備し、書き込み権限は追加しない。
- [x] 1.3 権限の拒否・一部許可・取り消しと再確認を実装する。
  起動時・画面復帰時・権限要求前後に状態を確認し、拒否・取り消しをクラッシュとして扱わず、再要求・設定への案内を用意する。取得直前・取得中の権限確認は Phase 2 の取得処理で実装する。最終画面の調整は Phase 7 で行う。

## Phase 2: Health Connect データ取得

完了（2026-09-14）: データ取得層とテストを追加。UI への取得結果の組み込み・表示期間の選択は Phase 6、日別の組み立ては Phase 3 で行う。

* `HealthDataRepository(AndroidHealthDataSource(context))` の `read(HealthDataRange(...))` で取得する。範囲はローカル日付の開始日を含み終了日を含まない形式で、明示した ZoneId から Instant に変換する。夏時間による23時間・25時間の日もテスト済み。
* `NutritionRecord.ENERGY_TOTAL` と `TotalCaloriesBurnedRecord.ENERGY_TOTAL` を公式 aggregate API で取得し、期間全体の kcal を返す。欠損は null のまま保持し、0 kcal と区別する。これは日別データやアプリの累積収支ではない。Phase 3 では各日の範囲に対応する集計を取得し、期間全体の値を各日に再利用・二重加算しない。
* 総消費カロリーの提供元優先順位・重複処理は Health Connect に委ねる。栄養は API が提供元をまたいで合算するため、同じ食事を複数アプリが記録した場合まで自動排除できるとは扱わない。アプリ独自の提供元選択・同一食事判定は実装していない。
* 体重は readRecords の全ページを取得する。null／空文字の終了トークン、空ページの継続、トークン循環を扱う。ID・測定日時・記録時オフセット・kg・提供元・更新日時を保持し、同じ ID の再出現だけをまとめる。異なる ID の同時刻測定は残し、日代表値の選択は Phase 5 に委ねる。
* 利用不可・更新必要・不足権限・アクセス拒否・取得エラー・空・データありを区別する。取得前、各ページ前、返却前に権限を確認し、途中失敗時は部分データを成功として返さない。キャンセルは伝播し、健康データのログ・永続保存・外部送信・書き込みは追加していない。
* 履歴権限は追加しない。既定では初回権限付与の30日前より古いデータに制限がある。付与日時を「現在から30日前」と推測せず、要求範囲を勝手に切り詰めない。API のアクセス拒否は空データと区別する。Empty は読み取り可能な範囲で API がデータを返さなかったことを表し、アクセス制限外にもデータがないことを保証しない。

検証: `test lint assembleDebug :app:connectedDebugAndroidTest --continue --console=plain` が成功。JVM 19件（追加13件）、API 34 の Android テスト6件（実 API 読み取り1件を追加）が成功し、失敗・スキップ0件。lint はエラー0件・既存警告16件。実 API テストでは直近2日分を読み取り、テスト前に未許可だった3権限を実行中だけ付与し、終了後に未許可へ戻した。データの書き込みは行っていない。複数ページ・片方だけのカロリー・取り消し途中・アクセス拒否・再試行等は代替データによる JVM テストで確認した。実端末上で大量レコードや複数提供元のデータを作成した検証ではない。

参照: [公式 aggregate API と提供元優先順位・履歴制限](https://developer.android.com/health-and-fitness/health-connect/aggregate-data)、[raw データとページング](https://developer.android.com/health-and-fitness/health-connect/read-data)。使用中の connect-client 1.1.0 のソースも確認した。

- [x] 2.1 最小限のデータアクセス層と取得結果の表現を用意する。
  Phase 1 が前提。日付範囲・タイムゾーンを受け取り、データあり・空・権限なし・利用不可・取得失敗を区別する。UI に API 呼び出しを埋め込まず、テスト用の代替データを渡せる構造にする。不要なモジュール分割や大量のレイヤーは導入しない。
- [x] 2.2 摂取・総消費カロリーを期間指定で取得する。
  `NutritionRecord` と `TotalCaloriesBurnedRecord` の公式 API を確認し、raw record と aggregate result の用途を整理する。ページング、取得可能な期間、複数データ提供元や重複の API 上の扱いを確認し、取りこぼし・二重加算を防ぐ。空データ・片方のみのデータ・取得失敗を検証する。
- [x] 2.3 体重を期間指定で取得する。
  `WeightRecord` の日時・単位・値を後続の集計へ渡す。複数測定、空データ、ページングを検証する。日代表値の選択は 5.1 で実装する。

## Phase 3: 日単位集計・ドメインロジック

- [x] 3.1 日付範囲・日付境界・単位の共通処理を実装する。
  Phase 2 の取得契約を前提に、ユーザーのローカル日付とタイムゾーンで範囲を扱う。開始・終了の inclusive / exclusive、日付をまたぐ区間、夏時間、タイムゾーン変更を明示する。エネルギーと体重の単位変換を一元化し、境界・変換を JVM 単体テストする。
  実装済み: `HealthDataRange.days()` と `LocalDayWindow` でローカル暦日ごとの開始含む・終了含まない境界を提供する。固定24時間で分割せず、夏時間や飛ばされた暦日を表現する。`HealthUnits` で SDK のエネルギーを kcal、質量を kg に統一し、エネルギーの欠損を維持する。日付・単位の追加 JVM テスト8件と既存テスト、lint / assembleDebug が成功。
- [x] 3.2 日単位の摂取・総消費カロリーと欠損状態を組み立てる。
  `readDailyCalories` が各ローカル日の重ならない範囲を aggregate API に渡し、期間全体の値や raw を二重加算しない。取得失敗・権限喪失を欠測に変換せず、部分結果を返さない。`DailyCalorieData` が元の記録を保持し、pure Kotlin の表示処理で記録あり・補間・欠測を区別する。補間元の日付と変更可能な閾値を保持する。日別取得・欠測・補間の追加 JVM テスト18件、既存分を含む45件と test / lint / assembleDebug が成功。今回の端末テストは未実施。体重の日代表値と表示への接続は Phase 5・6 で行う。

### 確定した欠測・補間方針（2026-09-14、U6・U7）

* 摂取カロリーの未記録は欠測として示し、0 と扱わない。摂取が欠測の日の収支は未算出とする。
* 消費カロリーと体重の表示値は、前後の記録日の間を暦日単位で線形補間する。連続欠測は最大6日までとし、7日以上は補間しない。片側の記録だけでは外挿しない。
* 元データを上書きせず、すべての補完・推定値とそれを使った収支・累積値に由来を引き継ぎ、画面でも区別する。欠測を含む累積は不完全であることを明示する。記録の存在だけで一日の記録が完全とは判断しない。
* 閾値は `InterpolationPolicy` に集約する。将来の変更・設定化に備えるが、設定画面は今回追加しない。
* 移動平均は実測体重だけを計算対象とし、補間値を含めない。表示期間の端でも補間できるよう、Phase 6 では必要な前後の記録を取得してから表示範囲へ絞る。読み取り可能範囲外を推測で埋めない。

## Phase 4: カロリー収支・累積収支

- [x] 4.1 日単位のカロリー収支を pure Kotlin で実装する。
  Phase 3 が前提。摂取 − 総消費の符号、両方が 0、正負の収支、片方が欠損する場合を単体テストする。欠損を無条件に 0 に置き換えない。
- [x] 4.2 選択期間の累積カロリー収支を実装する。
  日付順に計算し、期間変更時の累積開始点を明示する。U7 に基づいて欠損を含む期間の値と完全性を表現する。空・1日・複数日・期間境界・欠損・数値誤差を単体テストする。
- [x] 4.3 「期間累積収支」の名称・開始0 kcal・表示期間変更時の再計算をモデルとグラフで明示する。
  全履歴は累積せず、表示開始日の加算前を0とする。各日の点は当日分を含む期間累積値。既存の欠測・推定の扱いを維持し、正負・0・1日・空・日付順・期間変更を検証する。ローリング収支・長期間の選択肢・体重換算は追加しない。
  実装済み（2026-09-15）: `PeriodCumulativeCalorieBalance` / `periodCumulative` で期間内の値と明示し、グラフに開始日・0 kcal の基準点を追加した。JVM テスト8件と期間切り替えの UI テスト1件を追加し、JVM 全81件・API 34 Android テスト全12件、test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaks が成功。欠測日以降は未算出、小計と推定表示は維持する。

実装済み（2026-09-14）: `CalorieBalanceCalculator` が Phase 3 の表示用データから日別収支と累積を計算する。いずれかが欠測なら日別収支は null。欠測日以降の累積も null とし、算出可能な日の小計を別フィールドで保持する（欠測を 0 とする完全な累積として表示しない）。欠測日・補間を使った日付と日別の元データを保持し、不完全性と推定の有無を個別に判定できる。記録の存在は記録漏れがない保証ではない。補間後に選択期間へ絞り、累積は選択期間から再計算する。浮動小数点の加算誤差を補正する。追加 JVM テスト11件と既存45件、test / lint / assembleDebug が成功。UI・Android API は変更しておらず、端末テストは今回未実施。画面表示は Phase 6 で行う。

## Phase 5: 体重・移動平均

- [x] 5.1 日単位の体重推移を組み立てる。
  Phase 2・3 と U1 の決定が前提。1日に複数測定がある場合の採用規則を実装し、日時順序・同日複数値・欠損日を単体テストする。測定のない日を 0 kg と扱わず、Phase 3 の共通補間処理を表示用に適用する。実測値と補間値を分離する。
- [x] 5.2 体重の移動平均を pure Kotlin で実装する。
  U2・U3・U8 を決定し、日数に基づく窓、計算に必要な表示期間前のデータ、開始直後の必要データ不足、欠損、長い測定間隔の扱いを明示する。実測値だけを使用し、補間値を平均に含めない。空・短い期間・連続測定・欠損・窓境界を単体テストする。
- [x] 5.3 移動平均期間を画面から計算へ渡す仕組みを用意する。
  決定したデフォルトと選択肢で再計算できるようにする。複雑な設定画面は導入せず、UI への組み込みは Phase 6 で行う。

実装済み（2026-09-14）: `WeightTrendCalculator` が取得済みの体重レコードと表示範囲を受け取り、日代表値・補間表示値・移動平均を返す。追加 JVM テスト11件と既存56件、test / lint / assembleDebug が成功。今回の端末テストは未実施。データ取得との接続・期間選択 UI は Phase 6 で行う。

確定仕様（U1・U2・U3・U8）:

* ローカル日の最後の実測体重を代表値にする。同時刻は更新日時、さらに ID の順で決定し、入力順に依存させない。同一 ID の再出現は最新更新を採用する。
* 移動平均は初期7日、選択肢は7・14・30日。当日を含む N 暦日内の実測日代表値だけを平均する。分母は実測日数で、1日以上なら算出し、日数不足を別途保持する。
* 窓内に実測値がない場合や連続7日以上欠測している日の平均は表示しない。欠測閾値は補間と共通の `InterpolationPolicy` を使う。測定再開後は窓内の実測値で再計算する。
* 計算には表示開始前の最大 N−1 日の記録も渡す。補間用の前後記録を含め、取得できない記録を推測で生成しない。補間値を実測値や平均の入力に使わない。
* `MovingAveragePeriod` を計算引数として渡し、グラフ表示範囲とは独立して選択・再計算する。

## Phase 6: メイン画面・グラフ表示

- [x] 6.1 表示期間と画面状態の管理を実装する。
  Phase 4・5 と U4 が前提。既存 `MainActivity` / Compose テーマを活用し、挨拶画面を置き換える。UI、非同期取得、計算を分離し、表示期間・移動平均期間の変更を反映する。loading / error と正常状態を表現し、古い取得結果による上書きや再描画ごとの再取得を防ぐ。
- [x] 6.2 チャートの実装方式を評価・決定する。
  U5 を決定する。既存 Compose で必要な描画・操作・アクセシビリティを実現できるか確認し、外部ライブラリは必要性と保守性を評価してから採用する。この計画ではライブラリを指定しない。
- [x] 6.3 日別・累積カロリー収支と体重を同じ時間軸で表示する。
  日付、符号、エネルギーと体重の異なる単位、凡例を明示する。表示期間の選択と連動させ、欠損や不完全な値を実測値として表示しない。日付の位置と計算結果が一致することを確認する。
- [x] 6.4 体重の移動平均と期間選択を表示する。
  実測体重と区別し、同一時間軸で比較できるようにする。期間変更、開始直後、欠損があるグラフを確認する。

実装済み（2026-09-14）:

* U4: 初期表示は今日を含む直近30日。7・14・30日から選択する。移動平均は独立した7・14・30日選択（初期7日）。選択は画面再生成時に復元する。
* U5: Compose 標準 Canvas を採用。MVP の日別棒グラフ・累積折れ線・体重と移動平均の折れ線を同じ日付位置の上下3段で描画し、共通の日付スライダーと数値詳細で操作・読み上げを補う。外部ライブラリは追加しない。
* `DashboardLoader` が Phase 2～5 を接続する。表示開始前の平均・補間用記録も読み込み、過去へのアクセス制限時は表示期間だけで再試行し、その制約を保持・表示する。今日より後は取得・外挿しない。
* `DashboardRoute` が IO 上で取得・計算し、期間変更や画面離脱では coroutine をキャンセルして古い結果を反映しない。再描画だけでは再取得しない。権限確認済みならメイン画面、それ以外は既存の接続・権限画面を表示する。復帰・再確認で権限とデータを読み直す。
* 欠測はグラフの線を途切れさせ、補間・推定は白抜き／破線と詳細のラベルで区別する。不完全な累積と算出可能日の小計を分け、平均の実測日数不足も表示する。loading・error・空データを表示する。健康データの書き込み・ログ出力・外部送信は追加しない。
* JVM テスト73件、test / lint / assembleDebug が成功。API 34 の Compose UI テスト4件を含む Android テストを検証し、実取得2件は権限復元後に直接 instrumentation 実行で成功した。Gradle の再実行ではアプリ削除後の権限未付与により実取得2件がスキップされたため、スキップを成功とは扱わず再確認した。システム画面での拒否・取り消しを含む状態別の仕上げは Phase 7 に残す。
* 最終版は API 34 に上書きインストール後、全 Android テスト11件を直接 instrumentation 実行し、失敗・スキップなしで成功。初期30日表示も起動画面で確認した。pre-commit と新規ファイルを含む gitleaks が通過した。

### Phase 6 追加計画: 単一プロットへの3系列統合（2026-09-15）

6.1〜6.4 の完了記録は従来の上下3段表示の履歴として維持する。新しい完了条件は、**1つの Chart コンポーネント・1つのプロット領域・1つの共通日付 X 軸に、日別収支の棒、期間累積収支の線、体重の線を同時に重ねて表示すること**。別カード、上下分割、タブ切り替えでは完了としない。6.5 の仕様決定は完了。既存の移動平均も4系列目として同じプロットに重ねる。6.6のデータ・座標変換は完了。6.7の単一プロット描画は完了。6.8の日付選択と詳細表示は完了。6.9の期間連動は完了。6.10の回帰テスト・API 34確認も完了。Phase 7の画面全体の仕上げも完了。Phase 8の品質確認も完了。MVP追加対応8.5〜8.8は完了。8.9のCIビルド成功とartifact生成を確認済み。Phase 9の最終検証・文書整備の状況は下記を参照する。

調査した既存構造:

* `AndroidHealthDataSource` がカロリーを aggregate、体重を readRecords で取得する。`HealthDataRepository.readDailyCalories` が `HealthDataRange.days()` ごとのローカル日付範囲で取得し、`DailyCalorieData.forDisplay` が表示用の欠測・補間状態を組み立てる。
* `CalorieBalanceCalculator` は摂取 − 総消費と期間内累積を計算済み。`CalorieBalanceSeries.periodCumulative`、開始0の `periodStartKilocalories`、欠測日・推定日・小計を再利用する。計算やモデルへの累積追加を再実装するタスクは作らない。
* `WeightTrendCalculator` が日代表値・表示値・実測だけの移動平均を生成し、`DashboardLoader` が同じ表示範囲の収支と体重を `DashboardData` にまとめる。`MainActivity` が初期30日の表示期間を rememberSaveable で管理し、`DashboardRoute` が期間変更時に取得・計算する。UI と loader の選択肢は7・14・30日に限定されている。
* `DashboardScreen.kt` の `DashboardContent` は `TrendChart` を3回呼ぶ。各 Canvas 内で座標計算と描画を行い、`PlotSeries` は値・推定フラグ・色等の並列リストを持つ。データの同日対応は現在リストの添字に依存している。

現在の描画機能と制約（app の Gradle 設定・version catalog・描画コードで確認）:

| 調査対象 | 現状と対応の必要性 |
| --- | --- |
| チャートライブラリ | 外部チャートライブラリなし。Compose 標準 Canvas による独自描画 |
| 棒＋複数折れ線 | 同じ Canvas に drawRect / drawLine を重ねる構造は取れるが、現在の `bars` 引数は全系列共通。系列ごとの描画形式へ拡張が必要 |
| 複数Y軸・同単位の複数スケール | 現在は全系列共通の min / max と y 変換1つのみ。軸割り当て・目盛り・系列別変換は独自実装が必要 |
| 一部の軸を非表示 | 現在は数値範囲のテキストのみで軸管理機能なし。座標変換と軸の描画を分離すれば対応できるが、尺度を隠して誤読させない設計が必要 |
| 選択・ツールチップ | 共通 Slider と3系列の数値詳細は実装済み。Canvas 上のタップ判定・ツールチップは未実装。日付選択を共有して詳細表示を再利用できる |

Canvas での実現可能性は既存描画構造からの判断で、統合版の実装・性能検証済みという意味ではない。外部ライブラリの導入は前提にせず、6.5 で必要性が明らかになった場合だけ別途評価する。

既存の欠測仕様と今回の境界:

* 摂取未記録は欠測のまま。表示用の摂取または消費が欠測なら日別収支は null、その日以降の期間累積も null。算出可能日の小計は別値で、累積の線に代用しない。開始0は期間境界であり、欠測日の0ではない。
* 消費・体重は既存仕様で前後の実測日から最大6日まで線形補間し、7日以上・片側しかない場合は補間しない。記録・補間・欠測を区別し、推定由来を収支にも引き継ぐ。取得失敗を欠測として扱わない。
* U10 の確定により統合グラフでも既存の最大6日間の補間を区別表示して維持する。新しい補完処理は追加しない。既存の7・14・30日の体重移動平均は U11 に従って同じプロットの4系列目として表示し、計算を再実装しない。
* 長期間選択、ローリング収支、体重換算は「MVP 対象外」の既存項目で管理する。高度な記録不完全期間の可視化、新しい推定補完・移動平均の機能拡張は本計画の必須項目にしない。

- [x] 6.5 単一プロットの軸・スケールと既存表示の扱いを決定する。
  ユーザー回答により U9〜U11 を下記の確定仕様として文書化した。左は日別 kcal、右は体重 kg、期間累積は右軸と7,000 kcal/kgで連動する独立尺度とする。基準体重の選択・体重なしの代替表示、既存補間、4系列目の移動平均、グラフ優先の配置を確定した。確認用データ例も下記に記載する。6.5では設計のみ完了し、コード実装・検証は後続項目で管理する。
- [x] 6.6 統合グラフの同日データ対応と座標変換を整える。
  6.5 が前提。`DashboardData` の既存結果から日付・日別 kcal・期間累積 kcal・体重 kg・欠測／推定情報を対応付け、必要最小限の表示用変換を UI の描画処理から分離する。クラス名は既存命名に合わせ、累積の再計算や汎用チャート基盤は追加しない。共通 X 座標、決定済み Y スケール、0基準、期間開始点を扱い、実値と描画座標を分ける。完了条件は同じ日付の3値が一致し、空・1日・全0・一定体重・正負・桁差・欠測でも座標が破綻しないこと。pure Kotlin の対応付け・スケールテストを追加し、既存計算テストを回帰実行する。
  実装済み: `ui/DashboardChartData.kt` の pure Kotlin の `DashboardChartProjector` が日付で4系列を対応付け、元データ・欠測・推定情報と正規化座標を分離する。左の日別尺度、右の体重・移動平均・期間累積の連動尺度、実測優先の基準体重と補間への代替、体重なしの独立累積尺度を実装。係数は初期7,000 kcal/kgで引数から変更可能。期間開始点は日別値と区別し、小計を累積へ代用しない。追加 JVM テスト15件、既存を含む96件と test / lint / assembleDebug、pre-commit / gitleaks が成功。描画・取得処理・依存関係は変更せず、画面への接続と端末確認は6.7以降で行う。
- [x] 6.7 3系列を単一 Chart・単一プロットへ統合する。
  6.6 が前提。`DashboardContent` の3回の描画呼び出しを統合し、共通の日付 X 軸上へ日別収支の棒・期間累積収支の線・体重の線を同時描画する。6.5 の軸・尺度・既存機能配置を反映し、棒は0から正を上・負を下へ描く。期間累積の開始0と当日までの値を区別し、初日欠測へ線をつながない。凡例・線種・色・単位・クリッピングを整え、欠測を跨ぐ線や小計の累積扱いを禁止する。完了条件は同一プロットで3系列が重なり、桁差がある例でも読めること。Compose UI テストと API 34 の描画確認で、チャートが1つであることに加え、実際の棒・移動平均を含む3本の線・軸・欠測表示を確認する。
  実装済み: `DashboardChart` の1つの Canvas に日別の棒・期間累積／体重／移動平均の線を重ね、6.6の座標を使用する。左は日別 kcal、右は体重 kg、累積は7,000 kcal/kgで右軸と連動し、右 kcal 目盛りは非表示。期間開始0・基準体重の由来・体重なしの独立尺度を明示し、欠測を線で結ばず、小計を代用しない。グラフ下に期間操作を配置した。API 34でデモデータの描画を確認し、同一プロット内の棒・3本の線と欠測後の累積線非描画を画像で検証するUIテスト2件を追加。JVM96件・Android14件、test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaks が成功。lintは既存警告16件・エラー0。タップ選択は6.8、期間連動の追加検証は6.9に残す。
- [x] 6.8 共通日付選択と3系列の詳細表示を統合する。
  6.7 が前提。グラフのタップ位置を共通日付へ対応させ、既存 Slider と選択状態を共有する。選択日の3値をまとめて表示し、kcal / kg、欠測、推定、未算出の期間累積と小計を区別する。新しい浮動ツールチップの作成は必須とせず、既存の詳細欄を活用する。完了条件はタップと Slider が同じ日付・値・選択位置を示し、読み上げでも確認できること。左右端・期間開始境界・1日・欠測日の選択、スクロールとの競合、アクセシビリティを単体／Compose UI テストで検証する。
  実装済み: Canvasのタップ位置を描画と同じ余白・日付区間で判定し、既存Slider・選択線・詳細欄と選択状態を共有した。開始境界は初日として扱い、欠測日も選択できる。4系列の詳細は6.6の日付対応モデルから生成し、欠測・推定・小計・移動平均の情報を保ったまま画面と読み上げ用説明で共有する。前の日／次の日のアクセシビリティ操作を追加し、1日表示ではSliderを無効にする。縦スワイプはスクロールを優先し、日付を変更しない。JVM4件・Compose UI5件を追加し、全JVM100件・API 34 Android19件が成功。test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaks・差分チェックも成功。TalkBack音声そのものの手動確認は未実施で、説明と操作のsemanticsを自動検証した。期間連動の追加検証は6.9に残す。
- [x] 6.9 表示期間変更と統合グラフ・選択状態を連動させる。
  6.8 が前提。既存 `MainActivity` → `DashboardRoute` → `DashboardLoader` の流れを再利用し、7・14・30日変更時に X 軸と3系列を同じ新期間へ更新する。期間累積は既存 calculator で新しい開始0から計算し、表示前の平均・補間用データを累積へ加えない。期間変更時の日付選択、loading、キャンセル、画面再生成の整合性を確認し、必要箇所のみ修正する。完了条件は連続切り替えでも旧期間の値・選択・軸が混在しないこと。`DashboardLoaderTest` と `DashboardScreenTest` を補強し、開始日・同じ日付の異なる累積値・3系列の日付一致を検証する。長期間 UI の追加は別タスクのままとする。
  実装済み: `DashboardRoute` の読み込み状態をloader・表示日数・平均期間ごとに分離し、新しい操作と古いグラフが混在しないようにした。選択日は`DashboardScreen`で日付として保存し、平均期間変更やloadingを挟んでも維持する。表示日数変更時は新期間の最終日を選び、保存日が範囲外なら最終日に戻す。既存loader・calculatorを再利用し、30→7→14→30日の同日4系列対応、表示前収支の除外、基準体重の再選択、平均のみの変更で収支・基準体重が不変であることを検証。JVM2件・Compose UI2件を追加し、全JVM102件・API 34 Android21件、test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaksが成功。UIでは取得待機を制御して旧リクエストのキャンセル・旧グラフ非表示を検証し、StateRestorationTesterで期間・選択日の保存復元を確認した。実Activityの回転を含む全体の手動確認は6.10に残す。
- [x] 6.10 統合グラフの回帰テストと端末確認を完了する。
  6.7〜6.9 が前提。`CalorieBalanceTest` の全負・全正・混在・0・1日・空・日付順・期間変更・期間前の収支除外・欠測の既存テストを再利用し、同日3系列対応と描画の不足だけ補う。`DailyCaloriesTest` / `WeightTrendTest` の補間・欠測の回帰と、全件なし・一部系列のみ・累積未算出・大きな桁差の UI を確認する。API 34 で同一プロット内の重なり、軸・凡例の可読性、選択・期間変更、画面回転・文字拡大を確認する。test / lint / assembleDebug、該当 Android テスト、pre-commit / gitleaks を実施する。完了条件は3系列単一プロット要件と決定済み仕様の検証結果を記録すること。Phase 7.2 はこの検証を再利用し、画面全体の不足状態のみ仕上げる。

  完了（2026-09-15）: 既存の計算・座標・描画・期間連動テストを再実行し、体重のみ、期間内が補間体重のみ、7日以上の体重欠測と摂取欠測、大きな累積値と文字2倍、実Activity回転のAndroidテスト5件を追加した。文字拡大で軸数値が切れる問題を実測文字幅による軸幅に修正し、期間ボタンは折り返すようにした。API 34の既存デモデータで単一プロット・棒と3本の線・両軸・凡例を縦横画面と文字2倍で確認し、回転後も期間・平均期間・選択日を保持することを検証。全JVM102件・Android26件、test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaksが成功（lint: エラー0・既存警告16件）。途中のエミュレータ応答停止はデータを初期化せず再起動し、全Androidテストを再実行して成功した。回転・文字設定は復元済み。Health Connectへの書き込みは行っていない。実Activity回転テストは読み取り権限と既存データのあるエミュレータを前提とする。API 33手動確認はプロジェクト方針により省略。

## Phase 7: エラー・空データ・権限状態

- [x] 7.1 接続・取得状態をメイン画面へ統合し、復帰導線を完成させる。
  Phase 1・2 の状態表現を再利用する。利用不可、権限なし・一部許可・取り消し、loading、取得エラーと再試行を区別し、権限付与後・画面復帰後に再表示できることを Android 環境で確認する。
- [x] 7.2 データなし・部分欠損の表示を完成させる。
  全件なし、カロリーのみ、体重のみ、摂取か消費のみ、欠損日、平均の計算材料不足を区別する。確定した U6・U7 と U8 に沿い、補完・推定であることを明示し、7日以上の欠測を補間表示しない。主要状態の Compose UI テストを追加する。

完了（2026-09-15）: 7.1は既存の接続・取得状態と復帰導線を再利用し、接続確認が成功するたびに再取得用の番号を更新することで、同じ権限・期間でも再確認からデータを読み直すようにした。取得失敗後の再試行、loading、利用不可・更新必要・権限不足・アクセス制限・接続／取得エラーの案内をCompose UIで検証。API 34エミュレータではadbで読み取り権限を2/3→0/3へ変更し、対応する画面と再付与・アプリ復帰後のグラフ再表示を確認した。元の3権限許可状態へ復元済み。システム権限ダイアログのボタン操作は今回再検証せず、既存Phase 1の確認を再利用した。7.2は全件なしの復帰案内、期間内の摂取・消費・体重欠測と平均日数不足の案内を追加。期間前の実測から計算できる移動平均だけがある場合もグラフを表示する。欠測を0扱いせず、最大6日補間・長期欠測・累積未算出と小計の既存仕様を維持した。Androidテスト7件を追加し、既存の欠測・補間・描画回帰を含むJVM102件・API 34 Android33件、test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaksが成功。lintはエラー0・既存警告16件。健康データの追加・外部送信や権限・依存ライブラリの追加は行っていない。API 33手動確認はプロジェクト方針により省略。

## Phase 8: テスト強化・品質確認

- [x] 8.1 計算と取得境界の回帰テストを補強する。
  各フェーズのテストを重複作成せず、収支、日別集計、累積、移動平均、空・欠損・同日複数データ、日付範囲、タイムゾーン、単位変換の不足を埋める。Android Framework 不要の計算は JVM テストを使用する。
- [x] 8.2 Android 依存部分と主要導線を検証する。
  Health Connect の利用可否・権限・取得と UI を、必要に応じて Android test / instrumentation test と実機・エミュレータで検証する。サンプルの package 確認だけでは完了にしない。
- [x] 8.3 プライバシー・セキュリティを点検する。
  最小読み取り権限、書き込みなし、健康データの不要なログ・端末外送信なし、シークレット埋め込みなしを確認する。`.gitignore` を尊重し、機密ファイルを強制追加しない。現状の `allowBackup=true` とサンプルのバックアップ設定を点検し、健康データを保存する設計が入った場合は不要なバックアップ対象にしない。raw data の不要な永続化は避ける。
- [x] 8.4 全体の test / lint / debug build と pre-commit / gitleaks を通す。
  検証手順は下記に従い、失敗を修正して最終 diff を確認する。hook や検出を迂回せず、実行できない検証は理由とともに報告する。

完了（2026-09-15）:

* 8.1: 既存の収支・累積・日別集計・移動平均・空／欠測・同日複数記録・DST／日付境界・単位変換テストを再利用。取得2ページ目の失敗時に途中データを破棄して先頭から再試行するケース、日別取得中の利用不可化で取得を止めるケース、保存時オフセットに依存しないローカル午前0時前後の体重・平均をJVMテスト3件で補強した。
* 8.2: API 34で実Health Connectの集計・体重取得とダッシュボード取得、接続・取得失敗、欠測、期間変更、日付選択、回転の既存Androidテストを再実行。プライバシー画面の現行説明とメイン画面への復帰テスト1件を追加。システム権限の取消・再付与はPhase 7の確認を再利用し、今回は変更していない。API 33手動確認は方針により省略。
* 8.3: Manifest・データ取得層・依存関係・ログ／保存／送信経路を点検。健康権限は読み取り3種類のみで、INTERNET権限なしをインストール済みパッケージでも検証した。アプリ本体にHealth Connect書き込み、健康データのファイル／DB永続保存・ログ出力・外部送信はない。画面復元で保持するのは表示期間・平均期間・選択日のみ。allowBackup=trueとサンプルXMLは現状健康データの永続保存がないため維持し、将来保存を導入する際はクラウドバックアップ・端末移行対象を再点検する。プライバシー画面の「まだ取得しない」という旧説明を現行の読み取り・端末内集計に修正。secrets.properties、key.properties、署名keystore、.env系のGit除外を追加し、check-ignoreで有効性を確認（.env.exampleは管理可能）。デモ投入ツールはエミュレータ専用の別ツールであり、今回は実行・変更していない。
* 8.4: 全JVM105件・API 34 Android34件、test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaksと最終差分チェックが成功。gitleaksは新規Androidテストを含むapp/srcも走査。lintはエラー0・既存警告16件。依存ライブラリの追加・更新は行っていない。

### MVP追加対応: 表示調整とAPKの自動ビルド

8.1〜8.4の完了記録は維持する。以下の5項目もMVP必須とし、8.5→8.6→8.7→8.8→8.9の順に個別実装する。8.7は8.6のテーマ確認が前提。8.9はUI変更から独立して実施可能。すべての完了後に9.1・9.2で最終確認・利用手順へ反映する。8.5〜8.8は完了。8.9はGitHub上のビルド成功・artifact生成の確認と、ユーザーによるAPKダウンロード・インストール成功の報告に基づき完了。Phase 9とMVP全体の完了条件も確認済み。

- [x] 8.5 再確認・Health Connect設定の操作を画面下部へ移す。
  `DashboardScreen`と`HealthConnectionScreen`を対象に、本文・グラフ・期間選択・詳細や状態説明の後の下部操作領域へ配置する。権限未付与・一部許可時も上部へ戻さず、短い画面では下寄せ、長い画面ではスクロールして到達できる構成にする。権限要求・再確認・設定・復帰の既存動作を維持する。完了条件: 通常・loading・エラー・空データ・権限不足で配置と到達性を確認し、文字拡大・横向きでも操作可能。既存UIテストのクリックを必要に応じてスクロール対応し、再取得・権限復帰の回帰を確認する。
  完了: DashboardScreenとHealthConnectionScreenで共有するScreenWithBottomActionsを追加。短い画面では操作を下寄せし、長い画面では本文・グラフ・詳細の後まで一緒にスクロールする。再確認・設定・プライバシーと操作エラーを下部へ配置し、権限不足時も設定・再確認の表示条件と既存動作を維持した。短いloading画面の下部配置、文字2倍・高さ300dpでの一部権限画面の到達性をAndroidテスト2件で追加。既存テストもスクロール対応し、再確認テストは読み込み完了後に配置を検証するよう修正した。JVM105件とAPI 34 Android36件が成功（全体実行で35件成功、再確認1件は待機条件修正後の限定再実行で成功）。test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaks・差分チェックも成功。lintは既存警告16件・エラー0。API 34の実画面でも詳細の後の再確認・設定を確認し、回転・再取得・欠測・プライバシー復帰の回帰を検証した。権限を実際に変更する操作は今回行わず、既存Phase 7の検証を再利用。API 33手動確認はプロジェクト方針で省略。
- [x] 8.6 端末のライト／ダーク設定に追従する表示を仕上げる。
  `EnetrendTheme`には`isSystemInDarkTheme()`とダイナミックカラーによる切り替えが実装済み。新たなテーマ切り替え機構を重複追加せず、メイン・接続・プライバシー画面、グラフの背景・軸・凡例・線、システムバーが端末の設定変更に追従し読めるか検証して不足だけ修正する。アプリ独自の設定UIは不要。完了条件: API 34でライト／ダーク切り替えと文字サイズ変更、画面復帰を確認し、必要なCompose UIテストを追加・更新する。確認後は端末設定を復元する。
- [x] 8.7 日別収支の棒を正負で暖色／寒色に分ける。
  8.6を前提に`DashboardChart`の現在の単色primaryによる描画を、プラスは暖色・マイナスは寒色にする。現在の淡い印象と折れ線を邪魔しない描画順・透明度を維持し、ダイナミックカラー下でも正負の色の意味を保つ。0は中立色の基準表示とし、欠測は描かず、補間由来の白抜き表示と推定の説明は維持する。凡例・詳細も整合させる。完了条件: 正負混在・0・欠測・推定の描画を両テーマで確認し、符号だけでなく棒の位置と数値でも区別でき、累積・体重・移動平均が読めることをUI／画像テストで検証する。
- [x] 8.8 体重移動平均を点マーカーなしの線だけで描画する。
  `DashboardChart`の共有line描画処理で、移動平均系列のみ各日の点・白抜き丸を描かないようにする。線種による他系列との区別、欠測区間を結ばない処理、実測のみの平均計算と日数不足の詳細表示は維持する。孤立した1点はマーカーを生成せず、値は日付詳細で確認できるようにする。実測体重・期間累積のマーカーへ影響させない。完了条件: 連続・欠測を挟む・1点のみの平均と他系列をUI／画像テストで確認し、共通日付選択と詳細表示の回帰も通す。
  8.6〜8.8完了: 端末テーマへの既存追従を再利用し、Android側にもvalues-nightのテーマを追加。正負の棒にはダイナミックカラーに左右されないライト／ダーク別の暖色・寒色を用意し、塗りのalpha=0.35と折れ線を後に描く順序を維持した。0は中立色、推定は正負色の白抜きとし、凡例に色の意味を追加。移動平均だけ点マーカーを無効にし、既存の点線・欠測を跨がない線・詳細値・日数不足表示を維持した。両テーマの棒の描画と、孤立した平均値／欠測区間で描画しないこと・連続した平均線の画像テスト2件を追加し、既存の単一プロット画像テストも配色へ追従させた。3項目の実装完了後に回帰をまとめて実施し、JVM105件・API 34 Android38件、test / lint / assembleDebug / assembleDebugAndroidTest、pre-commit / gitleaksが成功。初回Android実行のActivity終了待ちタイムアウトはエミュレータをデータ保持のまま再起動し、全38件を再実行して成功した。lintは既存警告16件・エラー0。API 34で実際のライト／ダーク切り替え、文字2倍、メイン・権限不足・プライバシー画面とシステムバー、ライトへ戻した後のグラフを画像で確認。端末設定はnight=no・font_scale=1.0、読み取り権限は元の3種類許可へ復元済み。API 33手動確認は方針により省略。
- [x] 8.9 master更新時にdebug APKを保存するGitHub Actions workflowを作成する。
  実装前は`.github`のworkflowなし。`.github/workflows/`へworkflowを追加し、`master`へのpush（mergeによる更新を含む）を起点に、リポジトリのGradle Wrapperで`:app:assembleDebug`を実行する。AGP・compileSdk・Gradleと整合するJDK／Android SDKを用意する（現状はdaemon JVM 25・compileSdk 37。実装時に再確認）。runnerと各Actionの対応バージョンは実装時に公式仕様を確認し、ローカル設定やリリース署名のシークレットを前提にしない。成功時に`app/build/outputs/apk/debug/app-debug.apk`をダウンロード可能なartifactとして保存し、APKがない場合は失敗扱いにする。保存期間は14日を初期案とし、リポジトリの許容設定を確認して明示する。実行元commitが識別できるartifact名と最小限の権限を設定する。APKをGitへcommitしたりGitHub Releaseへ公開したりする処理は含めない。
  完了条件: workflowの構文・起動条件・ビルド・artifactパスを検証し、GitHub上でmaster更新により成功したrunとAPKのダウンロード・API 34エミュレータへのインストールを確認する。master更新とpushはユーザーのGit運用に従い、Codexが実行確認のためにmasterを変更しない。GitHubで未実行ならローカル検証と区別して未確認事項として残す。9.2で入手方法・保存期間・debug版であることを説明する。
  実装・ローカル検証済み: `.github/workflows/debug-apk.yml`を追加。masterへのpushだけを契機にUbuntu 24.04でJava 25・SDK 37・Build Tools 36.0.0を用意し、Gradle Wrapperの`:app:assembleDebug`を実行する。checkout / setup-java / upload-artifactは公式タグから確認したSHAに固定し、contents: read・認証情報の非保持・30分タイムアウトを設定。APKは`enetrend-debug-<commit SHA>-<run attempt>`のartifactへ14日保存し、対象なしは失敗にする。Wrapperに実行ビットがないためbash経由で起動する。リリース署名・Release公開・Gitへの書き戻しは行わない。
  actionlint 1.7.12（公式配布チェックサム検証済み）、ローカルのtest / lint / :app:assembleDebug --no-daemon、pre-commit / gitleaksが成功。新規workflowはgitleaksで直接走査した。ローカルAPKの存在とAPI 34へのインストールを確認。アプリコード・Gradle設定は変更しておらず、Android全体回帰は今回は再実行していない。
  作成時点の未確認事項（以下のCI確認記録で更新）: GitHub上でのmaster更新による起動・Ubuntu runnerでのビルド成功・14日保存設定の適用・artifactダウンロード・ダウンロードしたAPKのインストール。workflowは未commit／未pushで、Codexはmasterへmergeしないため、8.9全体のチェックは未完了のまま維持する。ユーザーがmasterへmerge・pushした後、GitHubのActions → Build debug APK → 成功したrun → Artifactsから取得して確認する。debug署名はrunnerで生成されるため、ローカル版や別runとの署名が異なる場合がある。9.2では上書きインストールできない場合の扱いも説明する。
  参照: [artifactの保存期間・設定](https://github.com/actions/upload-artifact#retention-period)、[Javaセットアップ](https://github.com/actions/setup-java)、[Ubuntu runner環境](https://github.com/actions/runner-images/blob/main/images/ubuntu/Ubuntu2404-Readme.md)。
  CI修正（2026-09-16）: SDK導入時のFailed to find packageエラーは、workflowが存在しないplatforms;android-37を指定していたことが原因。ローカルSDKのpackage.xmlとGoogle公式repository2-3.xmlの両方で確認したplatforms;android-37.0へ修正した。compileSdk / targetSdkは37のまま維持する。修正直後はGitHub実行成功・artifact取得が未確認だった。

  CI確認（2026-09-16）: GitHub APIで[run 35007432976](https://github.com/puvon/enetrend/actions/runs/35007432976)の成功とartifact生成（35,355,862 bytes、有効期限あり）を確認。Codex環境のダウンロードAPIは未認証で401となったが、その後ユーザーからGitHubのAPKダウンロード・インストール成功の報告を受領。ユーザー確認に基づき8.9を完了とする。

## Phase 9: MVP 仕上げ

- [x] 9.1 一連の利用シナリオで MVP 完了条件を確認する。
  初回接続から権限付与、期間選択、収支・体重・移動平均の比較まで通して確認する。権限取り消し、空データ、取得失敗からの復帰も含める。
  検証（2026-09-16）: test / lint / assembleDebug / assembleDebugAndroidTestが成功（JVM105件、lintエラー0・既存警告16件）。API 34でAndroid全38件が成功し、実Health Connect読み取り、期間切替・累積再計算、4系列比較・日付詳細、回転、欠測・空データ、取得失敗からの再試行、再確認、プライバシー導線を確認した。初回はDashboardPeriodTestの画面破棄中にComposeライフサイクル例外で中断したが、該当1件の単独再実行と全38件の再実行が成功した。再現した場合は原因調査が必要。システム画面の許可・拒否はPhase 1のユーザー報告、権限取消・再付与はPhase 7のエミュレータ確認を再利用し、今回の再操作とは区別する。空データ・取得失敗はテスト用データソースで確認。API 33手動確認は方針により省略。pre-commit / gitleaks（新規READMEの直接走査を含む）・差分確認が成功。健康データの投入は行っていない。その後、ユーザーからCI生成APKのダウンロード・インストール成功の報告を受領し、残っていた8.9の確認が完了したため、9.1・Phase 9およびMVP全体を完了とする。この完了状態の更新では、ユーザー指示に従いテストを再実行していない。
- [x] 9.2 利用・開発ドキュメントを整備する。
  新規作成した [README.md](README.md) に、実装済み機能、起動・検証方法、必要な権限、カロリー収支の符号、欠損・移動平均の決定済み仕様と制約を記載する。TODO の完了状態を検証結果に基づき更新し、残課題を報告する。

## 各フェーズの検証

Phase 0 で実在する Gradle タスクと実行環境を確認し、各フェーズ完了時に変更に該当する単体テスト・lint・debug build を実行する。Android 依存の変更では端末での検証を追加する。Phase 8・9 で全体を確認する。

今回のタスク一覧で `:app:test`、`:app:lint`、`:app:assembleDebug` の存在を確認済み。リポジトリルートで以下を使用できる。

```powershell
.\gradlew.bat test
.\gradlew.bat lint
.\gradlew.bat assembleDebug
```

限定的な JVM テストには確認済みの `:app:testDebugUnitTest`、接続した Android 端末での検証には `:app:connectedDebugAndroidTest` を使用できる。各フェーズで test / lint / assembleDebug を実行済み。API 34エミュレータのAndroid検証結果は各フェーズの完了記録を参照する。

pre-commit は既存の `pre-commit run --all-files` を使用し、設定済み `gitleaks-system` が成功することを確認する。`pass_filenames: false` のため、指定ファイルだけの走査とはみなさない。新規ファイルが実際の検査対象に含まれることも確認する。システムの gitleaks と pre-commit が必要であり、設定ファイルの存在だけで成功扱いにしない。

## 実装前に決定する事項

当初の未決事項 U1～U8 は確定済み。欠測・補間は Phase 3、体重代表値・移動平均は Phase 5、表示期間・チャート方式は Phase 6 の確定仕様を参照する。追加の仕様判断が必要になった場合は、該当作業の実装前に整理する。

API の対応条件、必要な権限、ページング、集計 API の挙動などは、採用する公式 API の仕様を実装時に確認する技術事項として扱う。

単一プロット統合の確定仕様（6.5、ユーザー回答による）:

* U9: 左Y軸は日別収支 kcal、右Y軸は体重 kg。日別と期間累積の尺度は独立とし、期間累積は右軸の1 kg分の高さを7,000 kcal分の高さとして連動させる。期間累積用の右側 kcal 目盛りは表示しない。凡例と詳細では日別収支・期間累積収支・体重・移動平均を区別し、累積の詳細値は kcal のまま示す。
* 期間累積0の高さは、表示期間内の最初の実測体重に合わせる。期間内に実測が一つもなければ期間内の最初の既存補間値を使い、補間由来と分かるようにする。実測・補間ともなければ同じプロット内で累積を独立スケール表示し、体重連動ではないことを明示する。期間変更時は基準体重も選び直す。
* 描画座標への対応は `基準体重kg + 期間累積kcal / 換算係数(kcal/kg)`。これは表示上の位置合わせであり、実測体重・推定体重のレコードを生成せず、日別・累積の実値を変更しない。右軸の表示範囲は体重・移動平均・累積の描画位置と開始点を含める。左軸は日別収支と0を含め、双方の目盛り間隔や余白は実装時に可読性を確認する。
* 換算係数の初期値は7,000 kcal/kg。コード上の一か所で変更できるようにし、正の有限値として検証する。ユーザー設定UIと保存は MVP 対象外。係数は描画変換のみに使用し、医学的な体重予測として表示しない。
* U10: 統合グラフでも既存の最大6日間の体重補間を維持し、補間値と実測値を区別する。7日以上・片側だけの外挿は禁止のまま。摂取欠測・収支未算出・累積未算出・小計の既存ルールも維持する。
* U11: グラフを表示期間・計算期間の操作より先に配置し、その下に両期間の操作を置く。日別収支・期間累積収支・体重の3系列と、既存の体重移動平均を4系列目の線として同じプロットに重ねる。移動平均は体重と同じ右 kg 軸を使い、実測値だけで計算する既存仕様を維持する。

6.6〜6.10への反映・確認例:

* 6.6: 基準体重の実測優先／補間への代替／値なし、係数変更、同日4系列の対応と座標変換をテストする。例えば基準70 kg・係数7,000なら、累積0は70 kgの高さ、−7,000は69 kg、＋7,000は71 kgの高さとなる。日別−300と累積−14,000が混在しても左の日別尺度は右の累積尺度に引きずられない。
* 6.7: 棒1系列と線3系列を同一プロットへ描画し、移動平均は線種・凡例で区別する。グラフの下に期間操作を配置する。体重なしでも累積線は独立スケールで描き、存在しない体重を表示しない。
* 6.8〜6.9: 日付詳細に既存の移動平均も保持する。表示期間変更で累積開始0と基準体重を再設定し、移動平均の計算期間だけを変えても累積の開始日・基準体重が変わらないことを確認する。
* 6.10: 初日に体重なし・後日に実測あり、期間内は補間のみ、体重全欠測、累積初日欠測、全系列なしを確認する。右 kcal 目盛り非表示でも累積値の単位と尺度が分かり、棒と3本の線が読めることを API 34 で確認する。

## MVP 対象外

以下は MVP 完了条件に含めず、ユーザーが作業対象として指定した段階で仕様を確認して実装する。期間累積収支の対応で見送った拡張もここで管理する。欠測・推定の基本表示と実測体重・移動平均の同一時間軸表示は既存実装および Phase 7 の対象であり、MVP 対象外へ移さない。

- [ ] 将来拡張: グラフ表示期間を3か月・半年・1年・3年・5年へ拡張する。Health Connect の読み取り可能期間と権限、長期間取得・描画の性能を確認してから実装する。移動平均の計算期間とは別に扱う。
  表示期間を変更するたびに期間累積収支を開始0 kcalから再計算し、同じ日の値が期間によって異なることを検証する。暦月・暦年の境界と選択肢は実装前に決定する。
- [ ] 将来拡張: 直近30日などのローリングカロリー収支を追加する。
  表示期間の先頭を基準とする期間累積収支と区別し、各日の移動窓内の収支を計算する。窓の日数、表示方法、計算に必要な先行データ、欠測時の扱いを実装前に決定し、窓境界・欠測をテストする。
- [ ] 将来拡張: 期間累積収支を体重変化の参考値へ換算する。
  6.5 の7,000 kcal/kgによる描画上の位置合わせとは別に、参考体重変化を値として提示する機能。表示する値・換算方法と係数の適用範囲を実装前に決定する。実測体重や確定的な予測と混同させず、推定であることと欠測・補完由来の不確かさを示し、換算と欠測時の扱いをテストする。
- [ ] 将来拡張: グラフの kcal/kg 換算係数をユーザーが変更できる設定UIを追加する。
  初期値7,000のコード上の係数を再利用し、入力範囲・保存・初期値への復帰を実装前に決定する。変更が描画に反映され、収支の実値や健康データを変更しないことを検証する。

ユーザーアカウント、クラウド同期、独自バックエンド、食事・運動・体重入力、Health Connect への書き込み、広告、analytics、telemetry、SNS 共有、CSV export、複雑な設定画面は必須計画に含めない。追加する場合は別途ユーザーの明確な要件に基づいて計画する。

## MVP 全体の完了条件

- [x] 再確認・Health Connect設定が権限状態にかかわらず画面下部に配置され、スクロール・文字拡大時も操作できる。
- [x] 端末のライト／ダーク設定へ追従し、各画面とグラフが読みやすい。
- [x] 日別収支の棒が正は淡い暖色・負は淡い寒色で表示され、折れ線の可読性を保つ。
- [x] 移動平均が点マーカーなしの線で表示され、欠測と日付詳細の仕様を維持する。
- [x] GitHubでmaster更新時にdebug APKを自動ビルドし、保存期間を明示したartifactからダウンロードできる。

- [x] 必要最小限の Health Connect 読み取り権限だけを使用し、書き込みを行わない。
- [x] 摂取カロリー、総消費カロリー、体重を取得できる。
- [x] ローカル日付単位で正しい日別収支・累積収支を計算できる。
- [x] 体重推移と移動平均を表示できる。
- [x] カロリー収支と体重・移動平均を同じ時間軸で確認できる。
- [x] 日別収支の棒・期間累積収支の線・体重の線が、1つの Chart・同じプロット領域・共通日付 X 軸に同時表示され、対応する軸・単位・選択日の値を確認できる。
- [x] 権限なし・取り消し、データなし・部分欠損、利用不可、loading / error を扱える。
- [x] 主要な計算ロジックに単体テストがあり、test / lint / build が成功する。
- [x] pre-commit / gitleaks を通過し、シークレットを追加していない。
- [x] 健康データを不要にログ出力・外部送信せず、端末内で完結する。
