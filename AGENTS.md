# AGENTS.md

## プロジェクト概要

EneTrend は、Health Connect に保存されたデータを利用して、カロリー収支と体重変化の関係を可視化する Android アプリです。

このアプリは、既存の健康データを読み取り、分析し、可視化することに特化します。食事、運動、体重などのデータ自体を EneTrend から記録することは基本方針としません。

主に以下の Health Connect データを利用します。

* 摂取カロリー: `NutritionRecord`
* 総消費カロリー: `TotalCaloriesBurnedRecord`
* 体重: `WeightRecord`

カロリー収支は以下の定義を使用します。

```text
カロリー収支 = 摂取カロリー - 総消費カロリー
```

意味は以下のとおりです。

* 負の値: 消費超過
* 正の値: 摂取超過

主な機能は以下です。

* 日単位のカロリー収支表示
* 選択期間内の累積カロリー収支表示
* 同一時間軸上での体重推移表示
* 日々の体重変動をならすための移動平均表示
* カロリー収支と体重変化の関係を分かりやすく可視化すること

Health Connect は原則として読み取り専用のデータソースとして扱います。

---

## 開発環境

主な開発環境は Windows です。

リポジトリ内に異なる設定が明示されていない限り、以下を前提とします。

* Windows
* Android Studio
* Android SDK
* Android Emulator
* Git for Windows
* Codex CLI
* Kotlin
* Gradle Wrapper

WSL が利用可能であることを前提にしないでください。

通常の Android 開発に Docker を必要とする構成にはしないでください。

PowerShell または Windows のターミナルで直接実行できるコマンドを優先してください。

グローバルにインストールされた Gradle ではなく、リポジトリに含まれる Gradle Wrapper を使用してください。

例:

```powershell
.\gradlew.bat test
.\gradlew.bat lint
.\gradlew.bat assembleDebug
```

Gradle タスクを使用する前に、現在のプロジェクトでそのタスクが存在することを確認してください。

SDK、Kotlin、Gradle、Android Gradle Plugin、Java、各種ライブラリなどのバージョンは、リポジトリに定義されているものを確認してください。

根拠なくバージョンを決め打ちしたり変更したりしないでください。

---

## 作業前のリポジトリ確認

ファイル変更を伴うタスクでは、最初に必ず以下を実行し、現在のブランチと既存変更を確認してください。

```powershell
git branch --show-current
git status
```

`master` の場合は一切変更せず、ユーザーへ報告して作業を終了してください。自分でブランチを作成・切り替えして回避せず、ユーザーが feature ブランチを用意するまで待ってください。

Git リポジトリでない場合や、ブランチ確認に失敗した場合、detached HEAD など作業用ブランチを確認できない場合も、推測で変更を開始せず、状況をユーザーへ報告してください。

`master` ではなく、ユーザーが用意した feature ブランチであることを確認したら、`TODO.md`、作業対象、関連コードと既存設計を確認してから変更を開始してください。詳細は「Git 運用」「TODO.md」「Codex の作業ルール」に従ってください。

必要に応じて、少なくとも以下を確認してください。

* `settings.gradle.kts`
* ルートの `build.gradle.kts`
* 各モジュールの `build.gradle.kts`
* `gradle.properties`
* `libs.versions.toml` が存在する場合はその内容
* `AndroidManifest.xml`
* 既存のソースコード
* 既存のテスト
* `README.md`
* `TODO.md`
* `.gitignore`
* `.gitattributes`
* `.pre-commit-config.yaml`
* gitleaks の設定
* この `AGENTS.md`

ディレクトリ構成、Gradle タスク、依存ライブラリ、アーキテクチャ、命名規約などについて、確認せずに存在を仮定しないでください。

既存のプロジェクト方針がある場合は、それを優先してください。

---

## 変更範囲

変更は依頼されたタスクに集中してください。

以下のような変更を、依頼と無関係に行わないでください。

* 関係のないリファクタリング
* 関係のないクラスやファイルのリネーム
* リポジトリ全体の一括フォーマット
* 関係のない依存ライブラリのアップグレード
* 理由のない Android Gradle Plugin の更新
* 理由のない Gradle の更新
* 理由のない Kotlin の更新
* 理由のない Java の更新
* 理由のない SDK バージョン変更
* 理由のない Compose の更新
* 将来必要になるかもしれないという理由だけでの抽象化
* generated file の直接編集
* 依頼範囲外の仕様変更

問題を解決するために必要な、最小限で完全な変更を優先してください。

作業中に別の問題を発見しても、依頼されたタスクに必要でなければ勝手に修正範囲を広げず、必要に応じて作業結果で報告してください。

---

## Android / Kotlin 方針

アプリケーションコードには Kotlin を使用します。

現在のプロジェクトが Jetpack Compose を使用している場合、UI は Compose を基本とします。

以下を優先してください。

* Android 標準 API
* Jetpack
* 公式 Health Connect API

プラットフォームや Jetpack で十分実現できる場合は、不必要な外部ライブラリを追加しないでください。

既存の以下の方針に合わせてください。

* 命名規約
* package 構成
* コードスタイル
* ファイル構成
* アーキテクチャ

Kotlin の null safety を適切に利用してください。

不必要な以下の使用は避けてください。

```kotlin
!!
```

nullable な値は、できるだけ明示的に処理してください。

技巧的な実装より、読みやすい実装を優先してください。

過度な抽象化を避けてください。

単純な機能のために、大規模な汎用フレームワークや複雑なパターンを導入しないでください。

MVP 段階では以下の順序を重視します。

1. 正しさ
2. テストしやすさ
3. 読みやすさ
4. 保守しやすさ
5. 単純さ

---

## アーキテクチャ

UI、データ取得、ビジネスロジックを適切に分離してください。

特に以下の処理を Composable 内に直接埋め込まないでください。

* カロリー収支計算
* 日単位集計
* 累積カロリー収支
* 移動平均
* 日付単位のグルーピング
* データ正規化

計算ロジックは、可能な限り pure Kotlin の関数やクラスとして実装してください。

Android Framework に依存しない計算ロジックは、通常の JVM 単体テストで検証可能な設計を優先してください。

Health Connect 固有の処理と、純粋なドメインロジックは可能な限り分離してください。

UI コンポーネントは主に以下を担当します。

* 状態の表示
* ユーザー操作の受付
* 適切なインターフェースを介したアプリケーションロジックの呼び出し

Composable に大量のデータ加工処理を書かないでください。

名前の付いたアーキテクチャパターンを採用すること自体を目的として、不要なレイヤーを追加しないでください。

---

## Health Connect に関するルール

Health Connect は EneTrend の中心機能なので、特に慎重に扱ってください。

### 読み取り専用を基本とする

ユーザーから明示的に仕様変更の指示がない限り、Health Connect は読み取り専用として扱ってください。

不要な書き込み権限を要求しないでください。

明示的な要件がない限り、Health Connect に健康データを書き込まないでください。

### 最小権限

実際に存在する機能に必要な権限のみ要求してください。

無関係な健康データの権限を要求しないでください。

### 正常系として扱う状態

以下は例外的な異常状態ではなく、通常起こり得る状態として扱ってください。

* Health Connect が利用できない
* 必要な権限が未付与
* ユーザーが権限を拒否する
* 後から権限が取り消される
* 対象期間にレコードが存在しない
* 一部のデータ種類しか存在しない
* 一部の日にデータが存在しない

これらの状態でも、アプリが適切に動作するようにしてください。

### 利用する主なデータ

主に以下を使用します。

* `NutritionRecord`
* `TotalCaloriesBurnedRecord`
* `WeightRecord`

特定のメソッドやフィールドへ依存する実装を行う前に、現在使用している Health Connect ライブラリの API を確認してください。

### タイムゾーンと日付境界

日単位集計では日付とタイムゾーンの扱いが重要です。

常に以下を考慮してください。

* 端末のタイムゾーン
* ローカル日付
* 1日の開始境界
* サマータイムが存在する地域での遷移
* 日付をまたぐレコード
* タイムゾーン変更

ユーザーのローカル日付で集計すべきデータを、安易に UTC 日付でグルーピングしないでください。

日付境界に関するロジックは、可能な限り一か所に集約してください。

### 単位

エネルギーや体重の単位を暗黙的に扱わないでください。

単位変換は明示的に実装してください。

単位変換ロジックは一元化し、テスト可能にしてください。

以下のような単位の違いを明確にしてください。

* calorie
* kilocalorie
* joule
* kilogram

変数名から実際の単位が分からない設計を避けてください。

### 集計

レコードの二重集計を避けてください。

Health Connect API から取得しているものが、

* raw record
* aggregate result

のどちらなのかを理解してから集計してください。

重複する aggregate result をさらに合計しないでください。

集計ロジックを変更した場合は、期待する挙動を確認するテストを追加してください。

---

## カロリー収支

EneTrend で使用するカロリー収支の正式な定義は以下です。

```text
カロリー収支 = 摂取カロリー - 総消費カロリー
```

したがって、

```text
負の値 = 消費超過
正の値 = 摂取超過
```

です。

この符号を勝手に反転しないでください。

UI 上で「赤字」「黒字」「不足」「余剰」など別の表現を使用する場合でも、内部計算ではこの定義を一貫して使用してください。

コアとなる計算ロジックには、一つの標準表現を使用してください。

---

## 体重推移と移動平均

日々の体重には自然な変動があります。

そのため、移動平均は単なる見た目上の機能ではなく、EneTrend の主要な分析機能です。

移動平均の計算は UI レイヤーから分離してください。

移動平均を実装する際は、以下の挙動を明確にしてください。

* 選択期間の開始直後
* 体重記録が存在しない日
* 1日に複数の体重記録がある場合
* 移動平均期間分のデータがまだ存在しない場合
* 測定間隔に空白がある場合

仕様で明示されていない限り、測定値がない日に架空の値を補完しないでください。

欠損値の扱いはテストで確認してください。

---

## 日付・数値計算

健康データの分析では、小さな計算上の不整合がグラフの意味を変える可能性があります。

決定論的でテスト可能なロジックを優先してください。

特に以下に注意してください。

* 日付範囲の inclusive / exclusive
* 開始境界と終了境界
* LocalDate と Instant の違い
* 浮動小数点の累積誤差
* 単位変換
* 欠損値
* 1日に複数のレコードが存在する場合

同じ計算ロジックを複数箇所に重複実装しないでください。

コア計算には一つの信頼できる実装を用意してください。

---

## UI 方針

UI の主目的は、分析結果を分かりやすく表示することです。

装飾的な複雑さより、分析上の分かりやすさを優先してください。

グラフでは特に以下を比較しやすくしてください。

* 日ごとのカロリー収支
* 累積カロリー収支
* 体重
* 移動平均などで平滑化した体重推移

実装を簡単にするためだけに、分析上重要な違いを隠さないでください。

データが存在しない状態も明示的に扱ってください。

例:

* Health Connect の権限がない
* カロリーデータがない
* 体重データがない
* 対象期間のデータが不完全

実際には「データなし」である状態を、仕様上の根拠なく `0` として表示しないでください。

「データがない」と「実測値が0」は区別してください。

---

## テスト方針

ビジネスロジックを変更した場合は、原則として対応するテストを追加または更新してください。

特に重要なテスト対象は以下です。

* カロリー収支計算
* 日単位のカロリー集計
* 累積カロリー収支
* 移動平均
* 欠損データ
* 同一日に複数レコードが存在する場合
* 日付範囲の境界
* タイムゾーン境界
* 単位変換
* 空のデータセット

Android Framework を必要としないロジックについては、通常の JVM 単体テストを優先してください。

純粋なビジネスロジックを、不必要に Android 依存クラスへ移さないでください。

プラットフォーム固有の挙動を確認する必要がある場合は、instrumentation test や Android test を使用してください。

実装詳細をそのままなぞるだけで価値の低いテストは避けてください。

---

## 作業完了前の検証

コード変更後は、現在のプロジェクトで利用可能なチェックを実行してください。

代表的なコマンドは以下です。

```powershell
.\gradlew.bat test
.\gradlew.bat lint
.\gradlew.bat assembleDebug
```

ただし、実際のプロジェクトを確認し、存在するタスクを使用してください。

作業途中では、変更箇所に対応したより限定的なテストを先に実行して構いません。

タスク完了前には、必要に応じて以下を確認してください。

* 単体テスト
* lint
* コンパイル
* debug build

実際に実行していないコマンドを「成功した」と報告しないでください。

実行できなかったチェックがある場合は、その理由を作業結果で明示してください。

テスト失敗や lint エラーを無視しないでください。

---

## Git 運用

このプロジェクトの基準ブランチは `master` です。

機能追加、バグ修正、リファクタリング、設定変更、ドキュメント更新など、リポジトリのファイルを変更する作業は、必ずユーザーが用意した feature ブランチ上で行ってください。

### master では変更禁止（必須）

Codex は `master` ブランチでファイルを変更してはいけません。これは推奨ではなく必須ルールです。

作業開始前に必ず以下を確認してください。

```powershell
git branch --show-current
git status
```

現在のブランチが `master` なら、ファイル変更や実装を開始せず、以下の趣旨を報告して作業を終了してください。

```text
現在 master ブランチのため、変更は行っていません。
このプロジェクトでは変更作業は feature ブランチ上で行うルールです。
feature ブランチを作成してから、再度実装を指示してください。
```

以下は禁止です。

* `master` 上でコードやその他のファイルを変更する
* `master` 上で commit する
* `master` へ直接 push する
* Codex 自身で feature ブランチを `master` に merge する
* `master` 上で変更したあと feature ブランチへ移動して帳尻を合わせる

### feature ブランチと役割分担

feature ブランチの作成と `master` への merge はユーザーの役割です。Codex はユーザーが用意した feature ブランチ上で実装と検証を行い、結果を報告します。

一つのまとまった変更ごとに、ユーザーが作業内容の分かるブランチを用意します。以下の `fix/` も含め、作業用ブランチを feature ブランチとして扱います。

例:

```text
feature/health-connect
feature/dashboard
feature/calorie-balance
feature/weight-chart
fix/weight-aggregation
```

Codex がブランチ名を決めて新規作成する必要はありません。

関係のない作業を同じブランチへ混ぜないでください。

必要に応じて、最近のコミット履歴も確認してください。

### ブランチ作成・master への merge 禁止

Codex は Git ブランチを作成しないでください。以下の操作は実行しないでください。

```text
git switch -c ...
git checkout -b ...
git branch <new-branch>
```

Codex は `master` へ切り替えて feature ブランチを merge しないでください。以下を通常の作業手順として実行しないでください。

```text
git switch master
git checkout master
git merge ...
```

`master` への merge を明示的に依頼された場合も、通常の作業手順としては行わず、このプロジェクトではユーザー自身が merge を行う方針であることを伝えてください。

### 既存のユーザー変更

作業開始時点ですでに存在していた変更を破棄しないでください。

未コミットの変更が Codex によるものだと決めつけないでください。

ユーザーが変更中のファイルが存在する場合、可能な限りその変更を維持したまま作業してください。

### 破壊的な Git 操作

ユーザーの未コミット変更を破棄する操作や、`git reset --hard` などの破壊的な操作を行わないでください。

以下を含みます。

```text
git reset --hard
git clean -fd
git checkout -- <file>
git restore <file>
git rebase
git commit --amend
git push --force
```

明示的な指示なしに履歴を書き換えないでください。

### commit / push

ユーザーから明示的に依頼されない限り、自動的に commit しないでください。

ユーザーから明示的に依頼されない限り、自動的に push しないでください。

commit / push を明示的に依頼された場合も、実行前に現在のブランチを確認し、ユーザーが用意した feature ブランチ上でのみ行ってください。`master` 上での commit と `master` への直接 push は行わないでください。

push の対象は feature ブランチのみとし、送信先が `master` でないことを確認してください。`git push --force` は行わないでください。

commit を依頼された場合は、そのタスクに必要な変更だけを含めてください。

commit 前には staged changes を確認してください。

---

## シークレットと機密情報

シークレットは厳格に保護してください。

以下をリポジトリへコミットしないでください。

* パスワード
* API token
* access token
* refresh token
* private key
* service account credential
* signing credential
* 秘密鍵を含む証明書
* Android 署名用 keystore
* 機密設定
* サンプルファイル内の本物のシークレット値

Android 開発で特に注意すべきファイル例:

```text
local.properties
secrets.properties
key.properties
*.jks
*.keystore
.env
.env.*
```

実際に無視されるかどうかは、現在の `.gitignore` を確認してください。

シークレットや機密ファイルを追加するために、以下を使用しないでください。

```text
git add -f
```

ビルドを通すためだけに、シークレットをソースコードへ移さないでください。

以下へ本物のシークレットを書かないでください。

* Kotlin ソースコード
* XML resource
* `strings.xml`
* Git 管理対象の Gradle ファイル
* README の例
* テストデータ
* ログ

ローカル設定が必要な場合は、既存のプロジェクト方式に従い、Git 管理対象外のローカル設定ファイルや環境変数などを使用してください。

設定例をドキュメントに示す場合は、プレースホルダーのみを使用してください。

例:

```properties
SOME_API_KEY=
```

サンプルファイルへ実値をコピーしないでください。

---

## gitleaks

このリポジトリには gitleaks が導入されています。

既存の gitleaks 設定を尊重してください。

以下を行わないでください。

* gitleaks を無効化する
* 検出ルールを理由なく弱める
* 検出を通す目的だけでファイルを除外する
* 広範囲な allowlist を追加する
* gitleaks のエラーを回避する

gitleaks がシークレット候補を検出した場合は、以下の順で対応してください。

1. 検出内容を確認する
2. 本物のシークレットか判定する
3. 必要であればシークレットを除去または置換する
4. 明らかな誤検知である場合のみ、必要最小限の allowlist を検討する

検出されたシークレットの実値を作業結果へ記載しないでください。

すでに本物のシークレットが Git 履歴に commit 済みの場合、現在のファイルから削除するだけでは不十分です。

その場合は、credential の失効・再発行や Git 履歴の修正が必要になる可能性があることを明示してください。

---

## pre-commit

このリポジトリには pre-commit が設定されています。

`.pre-commit-config.yaml` および設定済みの hook を尊重してください。

以下のような方法で hook を回避しないでください。

```text
--no-verify
```

pre-commit を迂回しないでください。`--no-verify` は使用しないでください。

hook がファイルを変更した場合は、

1. 変更内容を確認する
2. 変更が適切であることを確認する
3. 必要なチェックを再実行する

という手順で対応してください。

hook が失敗した場合は、hook を無効化するのではなく原因を修正してください。

commit を依頼された場合は、commit 前に設定済みの pre-commit チェックが成功することを確認してください。

ローカル環境で利用可能な場合は、必要に応じて以下を実行してください。

```powershell
pre-commit run --all-files
```

実際に実行していない場合は、「pre-commit が成功した」と報告しないでください。

---

## Health Connect データのプライバシー

Health Connect のデータはセンシティブな健康情報として扱ってください。

Health Connect のデータを、不必要に端末外へ送信する機能を追加しないでください。

明示的な製品要件がない限り、健康データに関して以下を追加しないでください。

* analytics
* telemetry
* crash report の追加データ
* remote API への送信
* cloud sync
* tracking

Health Connect の raw record を必要なくログ出力しないでください。

以下のような情報をログへ大量に出力しないでください。

* 個別の体重記録
* 詳細な食事データ
* カロリー履歴
* 個人を特定し得る健康情報

診断ログが必要な場合は、以下のような非センシティブなメタデータを優先してください。

* レコード件数
* 処理状態
* 一般的なエラー状態

debug log が健康データの実質的な持ち出し手段にならないよう注意してください。

明示的な機能要件がない限り、Health Connect の raw data を別途永続保存しないでください。

---

## ネットワーク利用

EneTrend にネットワーク依存を安易に追加しないでください。

ネットワーク通信やネットワーク対応 SDK を追加する前に、その機能に本当に必要か確認してください。

明示的な指示なしに以下を追加しないでください。

* 広告
* analytics
* tracking
* remote configuration
* telemetry SDK

端末内だけで実現できる機能については、ローカル実装を優先してください。

---

## 依存ライブラリ

新しい依存ライブラリを追加する前に、以下を確認してください。

1. Android 標準 API や既存依存関係で実現できないか
2. 本当に新しい依存ライブラリが必要か
3. 十分に保守され、信頼できるライブラリか
4. ビルドの複雑さやアプリサイズへの影響
5. 単純な処理のためだけにライブラリを追加していないか

通常の機能追加と無関係な依存ライブラリを同時に更新しないでください。

依頼内容に関係しない限り、version catalog や Gradle 設定を整理目的だけで変更しないでください。

---

## ドキュメント

コード変更によって開発者が知るべき情報が変わる場合は、必要に応じてドキュメントを更新してください。

対象例:

* `README.md`
* `TODO.md`
* この `AGENTS.md`

意味のある変更がない場合に、機械的にドキュメントを書き換える必要はありません。

ドキュメントは実際のリポジトリの挙動と一致させてください。

存在しないコマンド、モジュール、設定を記載しないでください。

---

## TODO.md

EneTrend の実装予定、実装すべき機能や修正内容は、原則として `TODO.md` に整理しておきます。

Codex は実装作業を開始する前に必ず `TODO.md` を確認してください。存在しない場合はその旨を報告し、Codex の判断だけで TODO や機能を追加しないでください。

原則として `TODO.md` の項目を上から順番に実施してください。ユーザーが特定の TODO 項目を指定した場合は、その項目を優先して構いません。

ユーザーから明示的な指示がない限り、複数の TODO 項目をまとめて勝手に実装しないでください。1回の作業では、指定された1つの機能・修正、または密接に関連するまとまりに集中してください。

TODO に記載されていない機能を、Codex の判断だけで追加しないでください。実装途中で新しい課題を発見した場合も、依頼された作業に必須でなければ勝手に実装せず、作業完了時に報告してください。

`TODO.md` 自体の変更（項目の追加・削除・並べ替え・状態更新を含む）は、ユーザーからの指示または現在の TODO 運用ルールに従ってください。ドキュメント更新の一般方針だけを理由に、勝手に変更しないでください。

有用な未完了項目や履歴を理由なく削除しないでください。

### 開発作業の基本フロー

```text
TODO.md に実装内容を整理
        ↓
ユーザーが作業対象を決める
        ↓
ユーザーが feature ブランチを作成する
        ↓
Codex が feature ブランチ上で実装する
        ↓
テスト・lint・build・pre-commit・gitleaks 等、該当する検証を実施する
        ↓
Codex が変更内容と検証結果を報告する
        ↓
必要に応じてユーザーが commit / push を行う
        ↓
ユーザーが master へ merge する
```

Codex に commit / push を依頼する場合は「Git 運用」の制約に従ってください。ブランチ作成と `master` への merge を Codex が自動で担当する運用にはしません。

---

## README.md

`README.md` はユーザーおよび開発者向けのドキュメントとして扱ってください。

実装変更によって以下が変わる場合は、README の更新が必要か検討してください。

* セットアップ方法
* 必要な権限
* 開発コマンド
* 対応機能
* 主要なアーキテクチャ

Codex 固有の作業ルールは README ではなく、この `AGENTS.md` に記載してください。

---

## Codex の作業ルール

ファイル変更を伴うタスクでは、以下の順序で進めてください。ブランチと既存変更の確認、および `master` での変更禁止は必須です。

1. ユーザーの依頼内容を確認する
2. `git branch --show-current` で現在のブランチを確認する
3. `git status` で既存変更を確認する
4. `master` なら一切変更せず、ユーザーへ報告して終了する。ブランチ確認ができない場合も変更せず報告する
5. ユーザーが用意した feature ブランチであることを確認したら、`TODO.md` を確認する
6. 指定された TODO / 作業対象を確認する
7. 関連コードと既存設計・実装パターンを確認する
8. 必要最小限の変更を実装する
9. 必要なテストを追加または更新する
10. test / lint / build / pre-commit / gitleaks 等、該当する検証を行う
11. 最終 diff を確認する
12. 変更内容と検証結果、および作業中に発見した対象外の課題をユーザーへ報告する
13. Codex 自身では `master` への merge を行わず、ユーザーに委ねる

ファイルを編集する前に、そのファイルの役割を理解してください。

リポジトリから確認可能な情報を推測で決めないでください。

ドキュメントが古く、現在のソースコードや設定と矛盾する場合は、現在の実装を確認したうえで判断してください。

実装方針に曖昧さがある場合は、

* 既存コードとの整合性
* 単純さ
* 現在の製品要件

を優先してください。

自動フォーマッタが利用できるという理由だけで、無関係なファイルまで変更しないでください。

build を通すためだけに警告を抑制しないでください。

技術的な理由がない限り、動作している既存コードを好みの書き方へ置き換えないでください。

---

## コメント

コメントは、コードだけでは分かりにくい理由や制約を説明するために使用してください。

コードをそのまま日本語や英語で言い換えるだけのコメントは不要です。

コメントが有効な例:

* Health Connect 固有の特殊な挙動
* 日付境界に関する判断理由
* 単位変換の前提
* 互換性対応の理由

明確なコードで表現できる場合は、長い説明コメントを避けてください。

---

## エラー処理

ユーザー操作やプラットフォーム上で普通に発生する状態は、明示的に処理してください。

通常の状態を例外処理だけで制御しないでください。

少なくとも以下を区別してください。

* 機能が利用できない
* 権限がない
* データがない
* データが想定外
* 本当のアプリケーションエラー

ユーザー向けメッセージは、可能な限り分かりやすくしてください。

内部例外の詳細やセンシティブな健康データを、そのままユーザーへ表示しないでください。

---

## パフォーマンス

過度な最適化は不要ですが、Health Connect のデータを明らかに非効率な方法で処理しないでください。

特に以下に注意してください。

* 同じ Health Connect クエリの繰り返し
* recomposition ごとの再集計
* 不要に巨大な中間 Collection
* main thread 上での重い処理

時間のかかるデータ取得や集計を UI thread 上で実行しないでください。

非同期処理については、既存プロジェクトで採用されている Android / Kotlin の方式を優先してください。

必要性が確認できる前に複雑なキャッシュ機構を導入しないでください。

---

## 完了条件

通常の実装タスクは、該当する以下の条件を満たしたとき完了とします。

* 依頼された機能が実装されている
* 関係のない機能を変更していない
* 既存プロジェクトの設計・スタイルと整合している
* 必要に応じて UI とビジネスロジックが分離されている
* 必要なテストが追加または更新されている
* 該当するテストが成功している
* 該当する lint が成功している
* 該当する build が成功している
* 不要な依存ライブラリが追加されていない
* シークレットや機密ファイルが追加されていない
* gitleaks を迂回していない
* pre-commit を迂回していない
* Health Connect の権限が必要最小限である
* 健康データを不要にログ出力または送信していない
* 必要なドキュメントが更新されている
* 最終 diff を確認している
* 実行できなかった検証がある場合、それを明示している

---

## 作業完了時の報告

タスク完了後は、簡潔に以下を報告してください。

1. 変更した内容
2. 重要な実装上の判断
3. 実行したテストやチェック
4. 実行できなかったテストやチェック
5. 残っている懸念点や追加作業があればその内容

実際に成功を確認していないものについて、

* test 成功
* build 成功
* lint 成功
* pre-commit 成功
* gitleaks 成功

と報告しないでください。

特に問題が残っていない場合は、不要な追加作業を作り出さず、その旨を簡潔に報告してください。
